# 更新日志

本文件记录各版本的改动。版本号与 `metadata.yaml` 保持一致。

## v1.4.0（全局默认额度分私聊 / 群聊 + 限额页样式重整）

用户原话：「webui 中限额页面的样式得优化一下，全局默认需要分成私聊默认和群聊默认。
等级管理现在看着有点丑，群聊和私聊区分肉眼看着不是很明显。
配置弹窗样式和布局太丑了，导致很拥挤。」

### 问题

1. **全局默认额度只有一份**：旧版存的是 `scope_type='global', scope_id='*'` 的一组周期行，
   私聊和群聊共用同一组上限。但两者的记账口径本来就不同——私聊按「人」、群聊按「群」——
   想说「群里每个人每天 3 万，私聊给 10 万」根本配不出来。
2. **等级管理私聊 / 群聊混在一张表里**，只靠一列灰色 tag 区分，肉眼分不清谁是谁。
3. **等级编辑弹窗** 560px 宽塞了十几个表单项（权限 / 模型 / 切换 / 检测 / 额度），
   又长又挤。

### 全局默认额度拆分（数据与判定链改动）

- `llm_quota` 的全局行 `scope_id` 从固定 `'*'` 改为 **`'private'` / `'group'` 两份**
  （表结构不变，复用 `scope_id` 维度；`UNIQUE(scope_type, scope_id, period)` 天然容纳）；
- `store.migrate_global_quota_scene()`（启动时、第一次 `reload_rules()` 之前跑）：
  把旧 `global/*` 行复制成 `private` + `group` 两行再删掉 `*` 行，幂等；
- `gate.LayerRef` 新增 **`quota_id`（额度查找键）**，与 `scope_id`（权限链键）分工：
  全局层的 `scope_id` **恒为 `*`**（对象级指令总权限存的就是 `global/*`，
  直接改 `scope_id` 会让单条指令的全局禁用静默失效——自检 `[13]/[14]` 当场抓出），
  额度查找一律走 `quota_key`（`check_quota` / `quota_chain` 已同步）；
- `/quota` 写入接口：`scope_type=global` 时 `scope_id` 必须是 `private` / `group`；
- 规则导入兼容旧备份：文件里的 `global/*` 行自动展开成 `private` + `group` 两行，
  旧备份导入后行为不变。

### 限额页样式（`QuotaView.vue`）

- **全局默认额度**拆成「私聊默认」「群聊默认」两张并排卡片
  （窄屏自动单列），各自独立保存，卡头注明记账口径（按人 / 按群）；
- **等级管理**拆成两个带色条分区的表：私聊等级（蓝）/ 群聊等级（绿），
  「适用」列同步改蓝绿双色 tag，新增按钮挪进各自分区头；
- **等级编辑弹窗**从 560px 单列长表单改为 860px、四个标签页：
  基本信息 / 默认权限 / 模型与自助 / 额度模板，长说明文案也做了精简；
- 「额度说明」卡补充「全局默认分两份」的条目。

### 未分组对象的行为（本次排查确认，未改动）

「未分组」（没归到任何等级）的对象**不会突破限制**：

- **额度**：沿档位链落到「全局默认」——私聊用私聊那份、群聊用群聊那份；
- **LLM / 指令权限**：落到配置页的「默认策略」（默认放行，可改成白名单）；
- **切换模型 / 检测模型**：`Gate.switch_enabled` / `detect_enabled` 对未归级**一律视为不允许**
  （显式打开才放行），管理员不受限。模型路由未归级时不干预（走 AstrBot 默认模型）。

### 自检

`tests/test_gate.py`（新增全局层权限键 / 额度键分工断言）、
`tests/test_store.py`（新增 `migrate_global_quota_scene` 迁移与幂等断言、
导入统计 4→5、旧备份展开成两行）全部通过。

## v1.3.17（主题重整 + 固定语义色 + 配置项中文 label）

### 1. 主题：默认「靛蓝」改成正牌蓝，旧紫保留为「紫罗兰」

用户原话：「默认的主题颜色靛蓝，为啥我看着是紫色，你改一下吧，
紫色的也不要删了，可以再加点主题，两边都加一下。」

- `indigo` 重配色成真的蓝（强调色 `(37,99,235)`）；旧配色改名 **`violet`（紫罗兰）**保留 ——
  它已经在很多人的配置里，直接改掉会让卡片悄悄变色；
- 主题从 4 套扩到 **8 套**，与 model_panel 对齐：
  `indigo`（蓝，默认）/ `violet` / `teal` / `amber` / `sunset` / `crimson` / `rose` / `graphite`；
- 新增 `TONE_THEMES`：`error → crimson`、`alert → amber`、`ok → teal`，
  `ModelSwitcher.theme_for(tone)` 会**强制**换主题。

### 2. 固定语义色（报错红 / 告警橙）

用户原话：「能不能将一些提示卡片固定颜色主题，例如报错用红色，告警用橙色。」

- `_notice(..., tone=)`：撞车、「刚测过不重复打」= **橙（alert）**；
  「检测失败」「检测跑完了但取最新数据失败」从纯文本改成**红卡（error）**——
  原来这两条是最容易和「撞车」混在一起读的；
- 分不清的问题用一张表写进 README：**橙 = 有事但知道是什么事，等一会儿；
  红 = 出了没预料到的错，先看日志**。

### 3. 配置项下拉全是中文

用户原话：「user_gateway 的配置项 label 用中文，现在是英文。」

AstrBot 的配置 schema 本来就支持 `labels`（与 `options` 一一对应），本插件一直没填，
于是配置页的下拉显示的是原始值：`allow / deny / observe / enforce / backend / indigo…`。

- 五个枚举全部补上中文 `labels`（策略 / 指令策略 / 限额模式 / 头像来源 / 卡片主题）；
- 控制台「配置」页的枚举下拉改读 `labels`（没有才退回原值）；
- 值本身不变，已有配置不受影响。

### 自检

`tests/test_model_switch.py`：主题断言更新为八套，并检查 `TONE_THEMES` 的固定映射；
`tests/test_detect.py`：撞车 = 橙卡（`alert`）、检测失败 = 红卡（`error`）。

## v1.3.16（撞车时不再先发「检测中」；所有提示都走卡片）

### 需求（原话）

「『模型控制台那边已经有一轮检测在跑了，稍后再试～』：有任务在跑的时候，
这个消息也用卡片展示，并且不要输出『检测中...』的卡片。」

### 改动

- **提前探忙**：`ModelDetector.panel_busy()` 读对面 `external_available()` 新报的 `busy`
  （三态：`True` / `False` / `None` = 对面版本旧没报，**不能**当成忙把功能整个拒掉）。
  忙时直接回提示卡返回 —— 连冷却都不记，因为「检测中」那张卡根本不必发出去。
- **撞车兜底**（快照过期 / 同一秒两个人点）：`_run` 里遇到 `busy` 时
  - 回卡片：「模型控制台那边已经有一轮检测在跑了 · 这一轮没有检测任何模型 · 等它跑完再发一次」；
  - **撤回刚才记下的冷却**（`unmark`）：那一轮一个模型都没打，不该让这个人白等 30 分钟；
  - 不再把它说成「检测失败」。
- **所有「不是结果」的话都用卡片**（新增 `ModelSwitcher.build_notice_card` + `detect._notice`）：
  撞车、全在模型冷却里（「这些模型 5 分钟内刚测过，不重复打」）都改成
  「只有头 + 脚」的提示卡（头部大字就是那句话），渲染不出来自动退回纯文本。
- 提示卡**不自动撤回**：它是信息，不是一次性交互卡。

### 自检

`tests/test_detect.py` 新增：`panel_busy()` 三态、忙时不发「检测中」也不扣冷却、
开跑后撞车用卡片回复 + 冷却撤回 + 不当成失败、以及「刚测过」改走卡片。

## v1.3.15（检测：默认把整组测完；结果卡分清「本次」与「今日」）

### 需求（原话）

1. 「单次最多检测几个模型：这个配置项限制了 1-30 个好像不太好，默认 8 个也不太行，
   应该默认测试该等级分组所有可用模型…应该直接检测该分组所有可选模型一起测啊。」
2. 「测出来是红色的，但是有毫秒显示，成功率 2.3%，我也不知道最新检测出的是报错还是通过了。」

### 一、默认不限量

- `detect_max_models` 默认 8 → **0（不限）**，范围 1-30 → 0-100（0 = 不限）。
  限制的初衷是防成本失控，实际效果却是「分组里 15 个模型只测了前 8 个」，
  把一次体检拆成两次 —— 而用户要的恰恰是一眼看完整组。
  真管成本的是那两道冷却（人 30 分钟 / 同一模型 5 分钟），不是这个截断。
- 配了上限时才截断，且**必须在卡片头部写明**「按配置只测前 N 个，还有 M 个没测」——
  否则会被当成漏测（这类「静默少做一点」的体验正是本次被投诉的那一类）。
- 进度卡带上「**最坏等多久**」：默认不限量后一次就是十几个模型，
  没有 ETA 会像卡死。估时按**并发轮数**算（并发数与超时取自模型控制台的
  `external_available()`，拿不到按 3 / 45s 保守估）。

### 二、结果卡分清「本次」与「今日」

用户读不懂的根源：行底是红的（状态），右边却挂着 `800ms`（上一次**成功**的延迟）
和 `2.3%`（今天一整个窗口的成功率）—— 三个数字来自三个不同的时间点，没有一处说明来源。

- 每行新增**本次结论标签**：`本次通过`（绿描边）/ `本次失败`（**实心红**，放在最右）。
  只有失败才实心：一行里该喊的只有真出事的那几行；
- **失败的行不画任何延迟** —— 留着上一次成功的毫秒数正是绕晕人的地方；
- 通过的行显示**本次**探测延迟（不再是库里那个旧值）；
- 失败原因（超时 / 鉴权失败 / 连接失败…）顶到副标题最前，与 model_panel 同一套说法；
- 成功率一律标注统计范围：`今日 2.3%`（原来是光秃秃的 `2.3%`）；
- 头部汇总改成「本次检测：2 通过 / 1 失败」；
- 本次没测到的模型（被模型冷却跳过 / 被上限截断）不带本次标签，只显示历史数字与数据时间。

### 自检

`tests/test_detect.py` 新增：默认不限量（12 个模型一次全测）、配了上限才截断且写明、
进度卡 ETA（9 个 / 3 并发 / 45s → 「约 2 分钟」而不是 9×45s）、
以及本次结论与历史数字的分离（失败不留毫秒 + 原因前置 + 通过显示本次延迟 + 未测行回落到旧口径）。

## v1.3.14（卡片加宽 + 状态改用「行底横向渐变」）

### 需求（原话）

「1. 加宽一点（模型名字长了有点看不太全）2. 状态不要用点展示，可以用浅色从左往右的渐变背景。」
（两个插件的卡片一起调。）

### 改动

- **加宽**：`WIDTH` 880 → **1080**。候选名普遍是「供应商 · 组织/模型-版本」那种长串，
  还要和右侧的指标小字（延迟 / 成功率）+ 标签抢位置；去掉行首那颗点后又收回 22px，
  现在 1080 下 `NVIDIA · deepseek-ai/DeepSeek-V4-Flash` 能完整显示；
- **状态改用行底渐变**（`_hgradient` / `_row_bg` / `_row_left` / `HEALTH_TINT`）：
  每行行底 = **左端掺 `HEALTH_COLORS` 那一档 → 右端淡回中性底**的横向渐变，
  浓度 正常 `0.90` / 降级 `0.66` / 故障 `0.60` / 无数据 `0.88`。删掉 `_health_dot`
  与它在行内的调用（那是个 10px 小点，扫一眼分不出「正常 / 降级」）；
- 左深右浅：右边是模型名与指标，底色越接近中性越清楚；
- 当前使用的那行右端仍停在主题浅色（`soft`）上，所以「我在用哪个」照样一眼可见；
- 行底必须用 `alpha_composite` 贴（`draw.rounded_rectangle` 会在圆角处留直角）。

### 自检

`tests/test_detect.py` 新增四项像素断言：四档状态与「不带 health」的同一张卡逐像素求差
（差异最大的那个像素就是行底色，不用去猜行画在第几行），再比通道 ——
正常**偏绿**、降级**偏琥珀**、故障**偏红**、无数据是**冷灰**（蓝 ≥ 绿 ≥ 红）。

## v1.3.13（/切换模型检测：与「萌萌模型控制台」联动；切换卡片带延迟与数据时间）

### 需求（原话）

「联动一下模型控制台…我想在这个项目里面也能查看模型延迟和可用性以及成功率，就是『切换模型』指令，
然后还有一键检测功能；检测开始时发一张检测中的提示卡片、检测完成后发送最新的『切换模型』卡片，
指令就用『切换模型检测』。这个检测实际上是调用 model_panel 的模型检测，结果也存在那边，
咱们只是读取数据；检测的模型就是当前窗口允许使用的模型。然后在分组配置里面加一下能否使用检测指令，
默认关。然后就是冷却时间：管理员之外的人要有冷却时间，默认 30 分钟、可配置，防止频繁检测。
这个功能要提示用户安装 model_panel，如果没有这个插件功能就不可用。同一个模型每次检测间隔不能低于
默认 5 分钟、可配置。最好在『切换指令』卡片上标注出来模型延迟和连通性的最新时间，
因为不仅仅是检测，还有对话也会更新这个延迟。字可以小一点。」

### 分工（先说清楚为什么这么切）

- **检测不在本插件实现**：调模型控制台的 `external_detect()`，结果也由它落库（它的面板上记为
  「**插件联动**」）。本插件只负责挑哪些模型、什么时候能测、发什么卡片 ——
  探测与统计口径**只此一份**，两边各写一套迟早会「你那边绿、我这边红」。
- **两道冷却都配在本插件**（而不是对面）：模型控制台的指令目前一律管理员限定，
  而限流是「面向普通用户」的需求，配置项跟着用户向的这一侧走才会有人真的去调。

### 新增

**指令 `/切换模型检测`**（`detect.py` + `main.py`）

- 挑目标 = `/切换模型` 卡片上那份名单（= 本分组允许使用的模型），顺序也一致；
- 先回一张「**检测中**」提示卡（复用同一套头部：当前模型 / 分组 / 今日用量 + 「正在检测 N 个模型」），
  后台跑完再发一张**最新的「模型切换」卡片**（带刚打出来的数字），并**重新挂上选单** ——
  看完就能直接回序号切换，不用再发一次 `/切换模型`；
- 结果卡按 `model_switch_timeout_sec` 自动撤回，与普通卡片一致；
- 与对面插件的手动 / 定时检测**共用同一把锁**：撞车时返回 `busy`，本插件提示「稍后再试」
  （不排队：排队等来的检测已经过期了）。

**权限与冷却**

- 等级新增 **`detect_enabled`**（数据库 v11，**默认 0 = 不允许**）：在「限额 → 等级管理」的
  「允许检测模型」里打开。它与「允许切换模型」**是两个独立开关** ——
  检测会真打模型花额度，「能看」不等于「愿意花钱测」；**管理员不受它限制**；
- 指令冷却 `detect_command_cooldown_min`（默认 **30** 分钟，**仅非管理员**）；
- 模型冷却 `detect_model_cooldown_min`（默认 **5** 分钟）：按对面记录的 `last_probe_ts` 算，
  跨用户 / 跨群 / 重启都有效。冷却内的模型**跳过并在卡片上注明**，不排队；
- `detect_max_models`（默认 8）：单次检测的硬上限（每个都是一次真实调用）。

**切换卡片上的数据注解**（`model_card.py` / `model_switch.py`）

- 每个候选行右侧多两颗**小字**：延迟 + 成功率（`F_METRIC=19`，比模型名小一档）；
- 行首一颗**健康色点**（正常绿 / 降级琥珀 / 故障红 / 无数据灰）；
- 第二行写**这条数据是什么时候的**（`1 分钟前` / `2 小时前`）：刷新它的不只是检测，
  **真实对话也会**，所以要把新鲜度标出来。时间**放最前面**（放末尾会被截断）；
- 拿不到数据（没装对面 / 该模型没记录）时**整组指标不画**，而不是画一排 `-`。

**没装对面插件时**

- 指令直接回一句「需要先安装『萌萌模型控制台』」+ 仓库地址（**不静默、也不自己另起一套探测**）；
- 对面装了但**版本过旧**（没有 `external_detect`）也判成不可用 —— 不能等调用时抛 `AttributeError`；
- `/切换模型` 本身不受影响：注解拿不到就安静跳过（缺另一个插件不该让这张卡变成报错）。

### 顺带

- 控制台「等级管理」表格新增「可用检测 / 检测关闭」标签，编辑抽屉新增「允许检测模型」开关；
- 配置页新增「模型检测」分区（总开关 + 两道冷却 + 单次上限）。
- 当前使用那一行的副标题不再重复「当前使用」（右侧标签已经说了），把第二行让给数据时间。

### 自检

`tests/test_detect.py`（新增）：三种「不可用」都判成不可用、注解字段与时间前置、
模型冷却按 `last_probe_ts` 切分（真实对话不算「刚测过」）、指令冷却按人记且可关、
四种拦截的提示、正常流程「先回检测中卡 → 后台跑 → 发最新卡 → 重挂选单」、
对面忙 / 报错时的说法，以及带指标与不带指标两种卡片的渲染。

## v1.3.12（群专属模型：切换不再跨群串号；切换成功回执卡片）

### 现象

用户反馈两条：

1. **一个群切换，其他群都切换了** —— 群聊切换串号；并要求「给群聊也加个专属模型」，
   手动切换（控制台）与指令切换改的就是专属模型，私聊同理；
2. 切换成功也**渲染当前使用的模型的图片**，渲染不出来退文字，默认用图片。

### 根因（串号）

旧版把 `/切换模型` 的结果按**用户**存（`policy.feature='model_choice'`，群/私聊各一份）。
群里切一次 = 「这个人在**所有**群里都换了」—— 因为键里根本没有群号。

### 修法

**切换 = 改专属模型**（`model_switch.py` / `main.py`）

- 私聊切换 → `policy(user, feature='model')`（他的专属模型，仅私聊生效）；
- 群聊切换（仅管理员）→ `policy(group, feature='model')`（**群专属模型，对全群生效**）；
- 与控制台的「专属模型」选择器是**同一把键**：手动切换与指令切换互相同步、互相看得见；
- 优先级统一为：**专属模型 > 等级主 / 备用模型 > 系统默认**（旧的「用户自选」层废弃）；
- 控制台好友 / 群详情抽屉都有「专属模型」选择器（群抽屉是新增的），
  群抽屉还能看到群管理员在群里切的结果。

**数据迁移**（`store.migrate_model_choice`，启动时自动、幂等）

- 旧「私聊场景的切换选择」→ 迁成该用户的专属模型（**没有专属模型的才迁**，不覆盖管理员钉的）；
- 旧「群聊场景的切换选择」→ 直接删：旧语义本来就是错的（按人、跨所有群生效），
  没有可迁移的对象语义，用户在群里重发一次 `/切换模型` 即可；
- `model_choice` 的常量与导入白名单保留，旧备份文件仍可原样导入（导入后闲置）。

**切换成功回执卡片**（`build_success_card`）

- 切换成功（含「0 = 恢复默认」）后发一张**只有头 + 脚**的小卡片：
  头 = 现在用的模型 + 分组 / 今日用量，脚 = 结果一句话；`rows=[]` 由
  `render_model_card` 新支持（整卡全空仍返回 None）；
- 渲染失败 / 发送失败自动退回原文本回执 —— 功能不能挂在一张图上；
- 带参数直切（`/切换模型 2`）与回序号两条路径都走这个回执。

### 验证

`tests/test_model_switch.py`：

- [2]/[2.1]：群聊场景读的是**群专属模型**；专属模型不可用时当前标记正确回落；
- [5]：管理员在群里切换 → 落库为 `scope_type=group`（不再是按人跨群生效）；
- [6]/[7]：私聊切换写 `feature=model`（`model_choice` 不再被写入）；`0` 删专属模型；
- [12]：`POST /subject/model` 带 `type=group`（群专属落库 / 清除 / 非法 type 拒绝）；
- [17.1]：回执小卡片渲染成 PNG 且明显小于完整卡片；`rows=[]` 能渲染。

`tests/test_store.py`：

- 新增 `migrate_model_choice` 专项：私聊选择迁移、已有专属不被覆盖、群聊选择删除、幂等；
- 导入导出：group 层的 `model` 规则合法（导出、导入、成员层拒绝同步更新）。

三套自检全绿；前端重建（v1.3.12）、zip 结构不变。

版本 v1.3.11 -> v1.3.12。

## v1.3.11（修复 WebUI 第一次进配置页没数据）

### 现象

用户反馈：WebUI 的**配置页面第一次进去是空的**，得手动点一下「获取」或 AstrBot 的
「刷新连接」才出数据。总览页（落地页）同理。

### 根因

AstrBot 把插件页塞进 **sandbox iframe**，桥接对象 `AstrBotPluginPage` 由父页面
**异步**注入。原来的拉取只等「对象存在」就发请求，但此时父页面往往还没回过
`context`、消息监听也可能还没挂好 —— 第一发 `apiGet` 被静默丢掉（或等到超时才报错），
于是首屏空白；用户再点一下时桥接早已就绪，自然就有数据了。

### 修法

- `webui-src/src/bridge.ts` 新增 `whenBridgeReady()`：等到 `getContext()` 有值
  （父页面收到 iframe `ready` 后回的 `context` = 握手完成）才放行；某些版本不发
  context 也照常工作，则退一步「对象可用就返回」（最多多等 2.5s）。结果用模块级
  Promise 缓存，整个会话只握手一次。
- `api.ts` 的 `request()` 改用 `whenBridgeReady()`（不再只看对象存在），所有页面的请求
  都受益，不必逐个改。
- 配置页 / 总览页的 `load()` 加**首拉失败自动重试**（最多 3 次，递增退避）：即便握手
  时机仍差一点，也会自己补出来，用户不用去手动点。

### 验证

- 前端 `vite build` 通过（无 TS 报错）；逻辑改动集中在桥接等待与重试，无后端代码变动。
- 无法在本机对真实 AstrBot 跑端到端（桥接由宿主注入），但根因与修复路径对照宿主侧
  `PluginPagePage.vue` 的 `ready → context` 握手逻辑一致。

版本 v1.3.10 -> v1.3.11。

## v1.3.10（撤回时间与序号有效期合并为同一个数字）

用户看完 v1.3.9 的一句话：「要啊，肯定要联动，用同一个数字」。

### 改动

- **删掉 `model_card_recall_sec`**，撤回时间直接取「切换选择的等待时长」
  （`model_switch_timeout_sec`）：用户能回序号的时间一到，卡片就消失。
  两个值分开配迟早会不一致 —— 卡片先没了（用户莫名其妙），或者过期了还挂着
  （引着人去点一个已经无效的序号）。`ModelSwitcher.recall_sec()` 现在就是 `ttl()`，
  同一个数字只有一个来源。
- **新增布尔开关 `model_card_recall`（默认开）**：整体关掉撤回。
  想「不撤回」是一个开关的事，不该再引入第二个秒数（上一版的坑）。
- **卡片底部提示跟着说清楚**：开了撤回写「回复序号切换（60 秒后自动撤回）· 0 = 恢复默认」，
  关掉则回到「回复序号切换 · 60 秒内有效 · 0 = 恢复默认」；只读时仍然只说为什么不能切，
  不提撤回。
- 控制台「配置 → 模型路由」里同步换掉这两个键；`/info` 返回的 `recall_sec`
  改成实际生效值（取自 `switcher`，不再是原始配置）。

### 验证

`tests/test_model_switch.py` [18] 改成断言「联动」本身：

- 默认 `recall_sec() == ttl() == 60`；
- 把有效期改成 25 → 撤回时间跟着变 25（**不会一个 25 一个 60**）；
- 关掉 `model_card_recall` → 撤回 0、有效期仍是 25，且卡片底部**不再写「自动撤回」**；
- 开着时底部必须写出「25 秒后自动撤回」；只读时底部仍是原因文案；
- 发卡片时交给发送侧的秒数就是有效期。

版本 v1.3.9 -> v1.3.10。

## v1.3.9（头像调大 + 卡片到点自动撤回）

两条用户反馈：卡片上的头像太小、卡片看完就没用了应该自动撤掉。

### 头像（`model_card.py`）

- 直径 92 → **120**，白圈 8 → 9（群里扫一眼就能认出是谁的会话）；
- 顺带把**头部正文块也垂直居中**：头像比文字高时，文字不再缩在顶部（两端各自居中）。

### 卡片自动撤回（新增 `recall.py` + 配置 `model_card_recall_sec`）

- 新配置 **`model_card_recall_sec`**（默认 **60** 秒；`5 - 3600`；**填 0 = 不撤回**），
  控制台「配置 → 模型路由」里可改；
- **只有 QQ（aiocqhttp）能真撤回**（OneBot 的 `delete_msg`），其它平台自动退化成
  「只发不撤」—— 撤回这件事绝不会把卡片本身发不出去；
- 为什么要单独一个模块：AstrBot 的适配器发出消息后**把协议端返回的 `message_id` 扔了**
  （`aiocqhttp_message_event._dispatch_send` → `await bot.send_group_msg(...)` 没有接收返回值），
  而撤回必须知道它。`recall.py` 在这一次发送的**前后**临时把 bot 的发送方法包一层捞出 id，
  发完立刻还原（实例属性，作用域最小；包不上就静默跳过 = 不撤）；
- **宁可不撤，也不能撤错**：抓不到 id、或这段窗口里抓到 ≠1 个 id（说明还有别人在发消息）
  时不排撤回；
- 撤回是后台任务（失败只记 debug 日志），插件卸载 / 热重载时 `Recaller.close()` 会全部取消，
  不让任务去碰已经关掉的连接。

### 验证

- `tests/test_model_switch.py` 新增 [18]：`clamp_delay` 的边界（0 / 负数 / 字符串 / 太小 / 太大）、
  `message_id_of` 的三种包装层级与裸标量、发送后**补丁确实还原**、到点真的删、
  私聊路径、没有 `delete_msg` 时退回 `call_action`、非 QQ 平台只发不撤、
  抓到多个 id 时跳过、发送异常照旧抛给调用方、`close()` 取消待撤回任务；
- 配置链路：`ModelSwitcher.recall_sec()`（默认 60 / 配 0 / 配太小夹到下限）与
  `handle_command` 发卡片时把秒数交给发送侧。

版本 v1.3.8 -> v1.3.9。

## v1.3.8（卡片版面向 `astrbot_plugin_box` 看齐：头 + 身体 + 脚）

用户反馈「参考 box 那个项目的卡片渲染，你这个好看多了」+ 两条具体要求：
头部背景太淡、以及分段圆角不对。这一版把版面整个重画。

### 版面结构（`model_card.py`）

    ╭──────────────────────────────╮  ← 头：主题色**纵向渐变**，上两角圆角、下沿直边
    │ 模型切换                  ╭──╮│     当前模型 38px 最多两行；头像挂白圈、垂直居中
    │ OpenAI · gpt-4o           ╰──╯│
    ├──────────────────────────────┤  ← 直边对接
    │ ╭──────────────────────────╮ │  ← 身体：白底，候选行是浅灰圆角块 + 序号方块
    │ │ ① OpenAI · gpt-4o  ⟨当前⟩│ │
    │ ╰──────────────────────────╯ │
    ├──────────────────────────────┤  ← 直边对接
    │ ● 回复序号切换 · 60 秒内有效  │  ← 脚：浅色底、上沿直边、下两角圆角
    ╰──────────────────────────────╯

- **圆角只在最外侧**：头的下沿、脚的上沿都是直角，头看上两角、脚看下两角
  （`_seg_mask`：圆角矩形 + 一块方角补丁；整卡再套 `_card_mask`）。整卡一个圆角矩形
  再往里贴色块，接缝处会出现"两段圆弧互啃"的缺口；
- **头部不再是"淡到看不出来"**：现在是一层 `top → bottom` 的主题色渐变（借 box 的做法），
  文字用同色系深色保证对比度；四套主题各 8 个色值（`top/bottom/ink/sub/accent/soft/footer/border`）；
- 其余跟着 box 一起对齐：卡片本体加大到 880 宽、行高 76、序号方块 40、柔和投影
  （聊天气泡里有一点纵深）、2px 描边、脚注左侧小圆点 + 右侧品牌（提示太长时只留提示）；
- **存图前摊到白底**：直接 `convert("RGB")` 会把卡外的透明区变成纯黑、阴影变成黑边。

### 验证

`tests/test_model_switch.py`：

- 新增 [17]：`_seg_mask` 头/脚遮罩的方角与圆角逐像素断言、`_card_mask` 四角圆角、
  真卡片上「卡外是白」「头的下沿贴边仍有色（是直角）」「头部确实上了主题色（不再淡）」
  「脚的上沿贴边就是脚底色」；
- [11] 卡片宽度断言跟着改成 `WIDTH + SHADOW_PAD * 2`（画布含阴影留白）。

顺手修掉 `tests/test_store.py` 里一个**与本次改动无关的假红**：趋势分桶的期望值原来按
`range(0, 420, 60)` 算小时数，而真正写进去的行只有 `now / now-60 / now-120` 三行，
于是每天"整点后 2~6 分钟"这一段跑测试都会期望 2 个桶、实际只有 1 个 → 现在按真实偏移算。

版本 v1.3.7 -> v1.3.8。

## v1.3.7（群聊卡片用群头像；今日用量不再在 0 时消失）

### 现象

用户复核 v1.3.6 后提了两条：

1. 群聊里那张卡片的头像应该是**这个群的**，不是发言人的；
2. **卡片上没有今日 token 使用量**。

### 根因与修法

**头像**（`ModelSwitcher.avatar_for`，新增）

- 原来无论私聊群聊都取``user:sender_id``。群里挂发言人头像既有歧义
  （那张卡片讲的是「这个会话/这个群走哪个模型」），群里每个人发一次指令还会换一个头像；
- 现在按场景取：**群聊 = ``group:群号``、私聊 = ``user:对方``**；
- 只从本地缓存读的话，控制台没浏览过的群会退化成插件 logo，所以**本地没有就现抓一次**
  （`AvatarCache.ensure`，抓完落盘、之后离线也有）。抓取带 **3 秒超时** +
  **失败后 10 分钟的负缓存**：指令回执不能被头像拖住，离线环境也不该每次发指令都空等超时；
- 都拿不到才退回插件 logo（不留空位）。

**今日用量**（`today_of`）

- 上一版「今天完全没用过就不显示这一行」。用户因此以为功能没生效
  —— 尤其是刚装好、只测了几次指令（指令不消耗 token）的时候；
- 现在**查得到就一定显示**，包括 ``今日 0 tokens · 0 次对话``；
  只有**统计读不到**（旧库 / 查询失败 / 库未就绪）才降级不显示这一行。

### 验证

`tests/test_model_switch.py`：

- [14] 改为「0 也要显示」：``today_of`` 在 0 用量 / 脏值下返回 ``{0, 0}``，
  只有统计不可用才返回空字典；
- 新增 [16]：私聊读 ``user:10001``、群聊读 ``group:88888``（**不读发言人的**）、
  有缓存不抓取、没缓存现抓、抓不到退回 logo 且**短时间内不重试**（负缓存生效）、
  群聊卡片整条链路（describe → build_card）渲染正常。

版本 v1.3.6 -> v1.3.7。

## v1.3.6（卡片重画：浅色底 + 单一强调色，当前模型回到标题区）

### 现象 / 需求

v1.3.5 那版还是被吐槽「丑」「变蠢了」，另外「当前使用的模型」应该放在**顶部标题区**，
而不是列成一条「标签 / 值」的信息行。

### 根因（这一版真正要改的）

v1.3.3 → v1.3.5 的卡片一直在**加东西**：大面积高饱和渐变头、彩色填充胶囊、信息条 + 分隔线…
结果是「三块结构各有各的视觉重量，却没有重点」——一屏看下来最该突出的「当前模型」
反而被塞进信息条里，字号比标题小、还带着标签文字，显得又乱又廉价。

### 重画（`model_card.py`）

- **浅色卡片 + 单一强调色**：卡片白底 + 1px 描边；头部只是一层几乎看不见的主题淡色，
  靠**字号与字重**拉开层次，不再用大面积渐变（那是「PPT 封面」感）；
- **当前模型回到标题区**：小标签「模型切换」→ **当前模型（31px，最多两行，不截断）** →
  一行灰字「分组 · 今日用量」；头像在右上角（白色描边圈）；
- **候选列表去框线**：不再一行一个圆角方框，靠留白分组；当前那行用**淡主题底 + 左侧色条**标识，
  序号用强调色（当前行）/ 浅灰（其余）；
- **标签改描边胶囊**：只有「当前使用」是实心强调色，其余（专属模型 / 暂不可用）描边 —— 
  一行并排两个也不再花；
- **主题只换强调色**：`THEMES` 从「整套配色」收敛成 `{accent, soft, tint}` 三个值，
  内置 `indigo`（默认）/ `teal` / `amber` / `rose`；旧配置里的 `mint` / `sunset` 会回落默认主题
  （不会报错、也不会画不出来）。
- 底部提示前加一个强调色小圆点，收一下版面。

### 验证

`tests/test_model_switch.py` [11] / [15] 跟随新 API（`current=` / `meta=`）更新：
四套主题都能出图且互不相同、主题名写错回落默认、改配置真的换配色、
**超长当前模型在标题区折行**（对比卡片高度，不再被截断）、空标题也能画。

顺手修掉存储层自检里的一个**时间相关假红**：`用户统计曲线按天 1 个点` 与
`短区间趋势分桶 = 1` 写死了桶数，而种子数据的 ts 是 `now - i*60`——
每天 00:00-00:06 跑测试时靠后几条落在「昨天」，桶数就是 2，测试必然红
（本次就是在 00:01 跑出来的）。现在按「种子时间戳实际跨了几个本地日 / 小时」断言。

版本 v1.3.5 -> v1.3.6。

## v1.3.5（卡片改版：配色主题、当前模型单独一行折行显示、加今日用量）

### 现象 / 需求

用户看完 v1.3.4 的卡片后提了三条：

1. **配色太丑**（粉紫渐变 + 粉底行）；
2. **顶部「当前：xxx」被截断**——默认模型名是「供应商 · 模型」，二三十个字符塞在头部副标题里
   必然看不全，而这一行恰恰是用户最需要看全的；
3. 想在卡片上看到**今日 token 用量**。

### 改动

**版面（`model_card.py`）**

- 头部只留「标题 + 分组」（都是短信息）；**「当前使用」另起一块信息条，整行宽 + 最多折行两行**，
  不再被头像挤窄、也不截断；
- 信息条第二行是**今日用量**（token + 对话次数），没有用量就不显示这一行（不写「0 tokens」）；
- 候选行加了极浅的边框；**当前使用的那行单独染一层淡绿 + 绿边**，扫一眼就能找到；
- 折行策略：优先在 `· - / _ , :` 这些分隔符处断开（模型名基本是「供应商 · 模型-版本」结构），
  避免从单词中间劈开（之前会出现 `...very-lo` / `ng-name`）；
- 折行有上限（两行）+ 末行省略号兜底，长名字不会把卡片撑爆。

**配色（`model_card.py` 的 `THEMES`）**

- 默认换成 **indigo**（靛蓝 → 天蓝渐变 + 近白底，正文全中性色、只有标签带彩色）；
- 另外内置 **mint**（青绿）与 **sunset**（暖橙），用 `model_card_theme` 配置切换；
  主题名写错会回落默认主题（配置坏了也要能出图）。

**今日用量（`model_switch.py`）**

- 新增 `ModelSwitcher.today_of()`：私聊取**这个人**、群聊取**这个群**的今日 token / 对话次数
  （与总览、列表页同一口径，复用 `store.subject_stats`）；
- 任何异常（统计表没数据、旧库、查询失败）都返回空字典 —— 卡片少一行，但绝不影响切换；
- 纯文本兜底列表同样带上「当前使用」与「今日用量」。

### 验证

`tests/test_model_switch.py`：

- 新增 [14]：今日用量（统计不可用 → 空、群聊按群 / 私聊按人聚合、0 用量不显示、脏值不炸、
  数字缩写口径）；
- 新增 [15]：三套主题都能出图且互不相同、主题名写错回落默认、
  `model_card_theme` 配置改了就换配色、**长模型名换到第二行**（对比卡片高度）、
  折行工具的分隔符断行与省略号兜底。

版本 v1.3.4 -> v1.3.5。

## v1.3.4（/切换模型：等级「允许切换」开关默认关、群聊仅管理员、没配名单用兜底三项）

### 需求

v1.3.3 的 `/切换模型` 有两处不合预期：

1. 等级里只配了「可切换模型」名单就相当于放开了自助切换，**默认是放的**——
   应该反过来：**默认关，要放开得管理员显式打开**；关掉时**只读**（能看不能切）。
2. 没配名单时直接提示「未开放」，用户连自己现在用的是什么模型都看不到——
   应该**展示当前模型 + 系统默认模型 + 备用模型**。
3. 群里谁都能切（群共用一个会话），应该**仅管理员**能用。
4. **管理员不受影响**（既不受「群聊仅管理员」，也不受「等级开关」限制）。

### 后端

- **数据库 v10**：`quota_level` 新增 `switch_enabled`（INTEGER，**默认 0 = 只读**）。
  v1.3.3 的「名单非空 = 开放」语义作废：**开关决定能不能切、名单只决定能挑哪些**。
  升级后所有等级都是只读（用户仍能查看自己用的是什么模型），要放开就在控制台打开开关；
- **没配名单 → 兜底三项**：`当前生效的模型 / AstrBot 系统默认模型 / 等级备用模型`
  （去重、按这个顺序）。配了名单则以名单为准（名单是管理员划的圈）；
- **只读时也发卡片**：能看清「现在走的是哪个、还有哪些候选」，但**不建立序号会话**——
  所以只读时随手发的数字不会被这条指令截走（这是刻意为之，别改成「只读也记会话」）；
  `/切换模型 N` 直接回一句「只读：xxx，请联系管理员」；
- **群聊仅管理员**：普通成员在群里发指令 → 一句话拒绝（不发卡片、不记会话），
  提示可以私聊用；**管理员豁免**这一条与上面的开关（用于排查「为什么他切不了」）；
- **内核**：`Rules.level_switch_enabled` + `Gate.switch_enabled(rules, level_id)`——
  没归级 / 没配一律视为不允许（安全的默认：配置没读到也只是退回只读）；
- **控制台**：`/levels` 读写 `switch_enabled`（字段缺省 = 沿用原值，避免部分更新把它重置）；
  `/ping` 的 `model_switch` 增加 `switch_levels`（已放开切换的分级数）。

### 前端（控制台）

- 「限额 → 等级管理」编辑器新增**「允许切换模型」开关**（默认关，关时「可切换模型」下拉置灰），
  文案写清只读语义、群聊仅管理员、管理员豁免；
- 等级列表的列改名「自助切换」：关着显示「只读（默认）」，开着显示「允许 · N 个」/
  「允许 · 兜底三项」并列出候选；
- 顶部额度说明同步。

### 验证

- `tests/test_gate.py` 新增 [19]：开关默认关、按等级各管各的、脏 level_id 不抛异常、
  **开关与名单互相独立**（只配名单不再等于开放）；
- `tests/test_store.py`：schema 10、`switch_enabled` 默认 0、`True/"false"/0` 统一收口成 0/1、
  导入导出双向保留（关着的等级不会被带成真值）；
- `tests/test_model_switch.py` 新增 [4]（兜底三项与去重）、[5.1]（只读：发卡片但不记会话、
  回数字不拦不答、不写库）、[5.2]（群聊：成员被拒 / 管理员豁免并可按群聊场景落库 /
  私聊只读但管理员豁免）；[12] 控制台接口补开关的读写与「缺省沿用原值」。

版本 v1.3.3 -> v1.3.4。

## v1.3.3（新增「/切换模型」：用户在等级圈定的名单里自助换模型）

### 需求

等级里的「主模型」是管理员定的，但有些场景下「让用户自己在几个模型里挑」更合适
（比如 VIP 想在快模型和强模型之间自己权衡）。于是：

1. 在**等级**里配一份「可切换模型」名单；
2. 用户发 **`/切换模型`** → 列出他所在分组允许切换的模型，**Pillow 渲染成卡片**，
   并标记**当前使用的**那个；他自己的专属模型也一并列进去；
3. 用户回一个**序号**就切换成对应模型（`0` = 恢复默认）。

### 后端

- **数据库 v9**（`SCHEMA_VERSION 8 → 9`）：`quota_level` 新增 `switch_providers`（JSON 文本，
  存提供商 id 列表；空 = 不开放）。迁移只加一列、默认空串，**升级不改变任何既有行为**；
- **模型类配置分两把键**：`policy.feature='model'` 仍是「管理员钉的专属模型」（私聊生效），
  新增 `feature='model_choice'` 存**用户自己**切换的结果（按场景分开：`private` / `group`）。
  分键是为了能回答「这个模型是管理员钉的还是他自己挑的」，控制台也能分开展示与清除；
- **`route_model` 优先级**：用户自己的选择 → 好友专属模型（仅私聊）→ 等级主 / 备用模型 → 默认。
  用户刚亲手切过却被配置按回去，等于这个指令白做了，所以他的选择必须最优先；
- **名单口径与模型路由同源**（`Gate.model_level_of`）：私聊看好友等级、群聊看群等级；
  候选 =「等级名单 + 他的专属模型 + 当前生效的那个」，顺序按配置顺序固定
  （序号每次都跳的话用户根本没法记）；
- **新增 `model_switch.py`**：名单组装 / 当前模型判定 / 序号会话 / 按场景落库 / 文本兜底；
  **新增 `model_card.py`**：Pillow 卡片渲染（头图 + 序号角标 + 标签 + 底部提示）；
  自带一份 OFL 授权的中文字体（`assets/fonts/`），字体按「配置 → 自带 → 系统」找，
  **全都找不到就不发图**（宁可不发，也不发一屏豆腐块），由调用方退化成文字列表；
- **新增指令**：`/切换模型`（别名 `/模型切换`，参数可选：`/切换模型 2` 直接切）；
  另注册一个正则处理器接「纯数字」，键是 `umo + 发言人`、TTL 默认 60 秒：
  **没有待选会话时原样放行**（正常的数字聊天完全不受影响），有则切换并 `stop_event()`
  （否则那个数字还会被当聊天内容送给模型，用户会收到两段回复）；
- **控制台**：`/levels` 返回并接收 `switch_providers`；新增 `/subject/model-choice`
  （管理员兜底：查看 / 清除某人自己切换的模型）；`/ping` 增加 `model_switch` 概况
  （开关、开放该功能的分级数、已切换人数、卡片字体是否就位）；
- **顺手修掉一个老问题**：导入规则时 `feature='model'`（好友专属模型）被当成未知 feature
  直接跳过 —— 导出有、导入丢，换机器后专属模型全部失效。现在 `model` / `model_choice`
  都放行，且**不再过 `_norm_effect`**（它们的 effect 存的是提供商 id，被规整成 `inherit`
  就等于凭空清空）；
- **热更新**（`main.py` 顶部新增 `_reload_sibling_modules()`）：AstrBot 热更新只重载
  `main.py`，不会级联重载依赖模块，从旧版本热更上来时 `store.py` 还是旧代码、**新迁移不跑**。
  现在在导入依赖之前先把已加载的同级模块 reload 一遍（全新启动时是空转，无副作用）。

### 前端（控制台）

- 「限额 → 等级管理」编辑器新增**「可切换模型」多选**（留空 = 不开放），列表新增「可切换」列
  （悬停看完整名单）；额度说明里补了这条规则的说明；
- 好友详情抽屉的「专属模型」卡：显示「他自己切换的：xxx」+ 一键清除；
  「生效模型」文案补上这一层优先级（与后端顺序保持一致，否则界面会给出与实际不符的结论）；
- 配置页「模型路由」分区纳入三个新键（`model_switch_enabled` / `model_switch_timeout_sec` /
  `model_card_font`），不落进兜底的「其他」。

### 验证

- `tests/test_gate.py` 新增 [18]：名单归属（私聊好友等级 / 群聊群等级 / 未归级 / 顺序稳定 /
  与等级模型路由互相独立）；
- `tests/test_store.py`：schema 9、名单读写（去空去重、裸 id 容错）、
  `model` 与 `model_choice` 两把键互不覆盖、导出导入双向保留（含坏行跳过）；
- 新增 `tests/test_model_switch.py`：名单组装、当前标记、「不在名单里也要能标当前」、
  群 / 私聊两份选择、序号切换与恢复默认、越界不消耗会话、**别人发的数字不误触发**、
  TTL 上下限、开关关闭静默失效、卡片渲染出合法 PNG、文本兜底；
- 前端 `build_webui.ps1` 重新构建产物。

版本 v1.3.2 -> v1.3.3。

## v1.3.2（等级下拉各归各位：私聊只列好友等级，群聊只列群聊等级）

### 现象

好友详情抽屉里的「私聊等级」与「群聊等级」两个下拉，选项是**同一份**（都列好友等级）；
好友列表的两个等级列同样如此。直觉上「私聊等级」该只列私聊那套、「群聊等级」该只列群聊那套，
现在却互相能选 —— 而且群里真正生效的档位有时对不上号。

### 根因（两个问题叠在一起）

1. **前端**：两个下拉都拿 `kind='user'`（好友等级）列表，群聊那一份只是加了个「未分组」项；
2. **内核**：这才是根子 —— `quota_level` 的 `id` 是**全局唯一主键**，`kind` 只是控制台的分组
   标签，但判定内核把等级映射的键写成了 `(kind, level_id)`。
   一旦好友的「群聊等级」指向一个**群聊等级**（`kind='group'`），
   查表查的是 `("user", id)` —— 直接查空，该档位被**静默跳过**：
   界面看着配好了，群里却不生效，还查不出原因。

所以此前前端「只敢」两个下拉都列好友等级；而群聊档位想引用群聊等级根本走不通。

### 修复

**内核（`gate.py` / `main.py`）**：等级映射一律以 `level_id` 为键（`level_effect` /
`level_effect_group` / `level_command_effect(_group)` / `level_model` 五张表）：

- `Gate._level_key()` 返回 `int` id，不再拼 `kind`；
- `Gate.resolve_model()` 按 id 取等级模型；
- 群聊场景「先看群聊专属值、没配回落主值」的判断去掉 `layer` 限制（对两种 kind 同一套规则：
  好友等级可能配了两套，群聊等级的主值即群聊值）；
- **向后兼容**：id 全局唯一且旧数据 id 不变，所以原来指向好友等级的归级记录判定结果完全一致
  （`tests/test_gate.py` 全量回归通过）。

**前端**：

- 好友详情抽屉：「私聊等级」只列 **好友等级**、「群聊等级」只列 **群聊等级**；
- 好友列表：「私聊等级」列只列好友等级、「群聊等级」列只列群聊等级；
  移动端卡片同理（群聊档位名走群聊等级）；
- 顶部场景切换器现在也影响**等级筛选下拉**（私聊筛好友等级、群聊筛群聊等级），
  切场景时清空该筛选（两套列表不同，留着旧值会筛出空列表）；
- **历史数据兼容**：若某个好友的「群聊等级」存的仍是好友等级（v1.2.x 的语义），
  下拉里补一项 `xx（旧：好友等级）` 用于回显，换成群聊等级后自动消失；
- 说明文案同步（「私聊等级 = 好友等级、群聊等级 = 群聊等级」）。

### 验证

`tests/test_gate.py`：等级映射全部改用 id，并新增断言
**「好友的群聊等级指向群聊等级 → 群聊按它生效、私聊互不串」**（旧键在这条会失败）；
`tests/test_store.py` 一并回归通过。

## v1.3.1（修复：群详情的「近期流水」显示成了全库流水）

### 现象

打开「群聊」列表里任意一个群的详情抽屉，底部「近期流水」列出的是**与这个群无关**的记录
（别的群、甚至私聊），和上方「用量概览 / 曲线」的数字对不上。

### 根因

`webui_api.h_subject` 组装详情时，近期流水只对好友传了过滤条件：

```python
recent = await plugin.store.query_usage(
    from_ts=from_ts,
    to_ts=to_ts,
    sender_id=subject_id if subject_type == "user" else None,  # 群：一个条件都没传
    limit=50,
    offset=0,
)
```

而 `store.query_usage` 当时根本没有「按群过滤」的参数，群详情这条查询就退化成
「时间区间内**全库**最新 50 条」——谁最近用过 bot 就显示谁，与群无关。

### 修复

- `store.query_usage` 新增 `group_id` 过滤参数（`_usage_where` 同步支持；列名仍走白名单 + 参数化，无注入面）；
- `webui_api.h_subject` 改为按对象类型二选一：好友 `sender_id`、群 `group_id`；
- **为什么群按 `group_id` 而不是 `scope_type='group'`**：群里的流水会落在两种作用域上——
  命中群级规则时记 `scope_type='group'`，命中该用户的对象级规则时记 `scope_type='user'`，
  但 `group_id` 一律是群号。只有按它过滤才能把两类一次捞全，口径也与 `subject_stats` 对齐。
- 注意：用量明细页（统计页）的「对象 ID」筛选走的仍是 `scope_id`，按群号筛只会命中
  前半类流水（这是独立入口，未在本次改动范围内）。

### 验证

`tests/test_store.py` 补 3 条断言（按 `group_id` 过滤 / 覆盖 `scope_type='user'` 的群内流水 /
可与时间区间叠加），全量存储层自检通过。

## v1.3.0（权限展示全面平铺：私聊 / 群聊一眼分清）

### 好友详情抽屉

权限卡重构：删掉「私聊 / 群聊」场景切换器，改为**两个分组块平铺**——

```
┌ 私聊 ────────────────────────────┐
│ LLM 权限 [继承|放行|禁止]          │
│ 指令权限 [继承|放行|禁止]          │
│ 私聊等级 [未分组 ▾]               │
└──────────────────────────────────┘
┌ 群聊 ────────────────────────────┐
│ LLM 权限 / 指令权限 / 群聊等级     │
└──────────────────────────────────┘
```

每块内直接编辑，写的就是对应场景的规则，不存在「现在改的是哪套」的歧义。

### 好友列表

权限列从「跟随场景切换的两列」改为**固定四列**：
`LLM · 私聊` / `指令 · 私聊` / `LLM · 群聊` / `指令 · 群聊`，
每列的三态开关直接写对应场景的规则；顶部的场景切换器只影响**批量操作**。

**移动端卡片**的权限徽标同样平铺成四个（私 LLM / 私指令 / 群 LLM / 群指令）。

### 说明

- **好友专属模型的作用范围澄清（逻辑未变）**：只影响该好友的**私聊**；
  在群里聊天仍然走**那个群的模型**（群等级路由），不会用他的专属模型——
  模型卡说明文字已按此重写，避免歧义。

## v1.2.2（⚠️ 行为变更：群聊默认不再使用好友等级）

### 语义修正

v1.2.0 的「群聊等级」默认值是「跟随私聊」——但档位链是「命中最具体一层即止」，
好友只要归了级，群聊里「好友等级」层就会命中、**群等级被跳过**，
这与「群聊应该走群的档位」的直觉相悖。

**本版起**：好友的等级（无论私聊等级还是群聊专属等级）**只管私聊**；
群聊里他默认就是普通群员，走群的档位（群专属 / 群等级 / 全局默认）。
只有显式配了「**群聊等级**」才会在群聊里使用该等级。

### 界面

- 「群聊等级」的默认选项由「跟随私聊」改为「**未分组（用群的档位）**」；
- 详情抽屉与列表的提示文案同步更新。

### ⚠️ 升级注意

如果此前依赖「好友带着私聊等级的权限 / 额度进群」，升级后该行为会消失——
请给需要的人显式配置「群聊等级」。模型路由不受影响（群聊本来就按群等级）。

### 其他

- 好友列表「群聊等级」列宽 +10（选项文案变长）。

## v1.2.1（好友列表加「群聊等级」列 + 专属模型回显修复）

- **好友列表**：原「等级」列拆成两列——「**私聊等级**」与「**群聊等级**」，
  群聊列含「跟随私聊」选项；批量归级仍作用于私聊等级；
- **移动端卡片**：副标题同时显示私聊 / 群聊两档等级；
- **修复**：详情抽屉的专属模型保存后下拉框回显成「跟随等级配置」——
  回显字段读错了层级（`subject_model` 在 `model_route` 里），已修正；
- 专属模型下拉支持**搜索**。

## v1.2.0（好友等级分场景 + 好友专属模型）

### 1. 好友等级分场景（私聊 / 群聊各配各的）

此前一个好友只能归一个等级，私聊和群聊共用。现在：

- **详情抽屉**：权限卡拆成「私聊等级」和「群聊等级」两个下拉；
  群聊等级可选「**跟随私聊等级**」（默认，即旧行为）；
- **好友列表**：等级下拉跟随顶部场景切换器——私聊场景改私聊等级、
  群聊场景改群聊专属等级（多一个「跟随私聊」选项）；
- 生效优先级：群聊场景 = 群聊专属等级 > 私聊等级 > 未归级；
- 群对象归级不分场景（不变）。

### 2. 好友专属模型（优先级高于等级）

- 好友详情新增「**专属模型（私聊）**」卡：下拉选择提供商，**优先级高于等级里配置的模型**；
- 「**恢复默认**」按钮一键清除专属配置，回到跟随等级；
- 仅对该好友的**私聊**生效（群聊仍按群等级路由，维持「同一群上下文同模型」的设计）；
- 配置的提供商不可用（未加载 / 熔断）时自动回落等级路由 → 默认模型。

### 存储与实现

- 结构 v7 → v8：`subject_level` 表 `level_id` 放宽为可空、新增 `level_id_group`
  （群聊专属等级，NULL = 跟随私聊）；迁移走「建新表搬数据改名」，可重入；
- 判定内核 `Rules.user_level_id_of(uin, scene)`：群聊优先取专属等级，
  档位链 / 额度 / 权限全部经此解析，行为对旧数据完全兼容；
- 好友专属模型复用 `policy` 表（`feature='model'`），无新表；
  路由顺序：好友专属 → 等级路由 → AstrBot 默认；
- 自检：store 补分场景归级读写（含「私聊未归级但群聊归级」的混存）、迁移断言升到 v8；
  gate 补「群聊专属等级生效 / 未配跟随私聊」两条回归。

## v1.1.2（移动端详情「用量概览」样式优化）

- 详情抽屉的「用量概览」从挤爆窄屏的 4 列 `n-statistic` 改为 **2×2 迷你统计块**：
  浅灰底圆角小卡，标签在上、数值在下，窄屏不再换行错位；
- 桌面端同布局（2×2），视觉更整齐。

## v1.1.1（等级管理加类型筛选）

- 「限额 → 等级管理」头部新增筛选：**全部 / 好友（私聊）/ 群（群聊）**；
- 数据一次拉全、前端过滤（无额外请求），计数与空状态随筛选联动。

## v1.1.0（移动端卡片精简：只展示，不编辑）

按移动端惯例把卡片做成**纯展示**：

- 好友 / 群卡片上的 LLM / 指令权限从三态开关改成**只读徽标**
  （`LLM：放行` / `指令：禁止`，带颜色区分）；
- 等级继续显示在名称下方（`QQ · 等级名`）；
- **编辑全部收进详情抽屉**：点卡片进详情，那里有权限开关、等级、额度、模型路由；
- 群卡片的「成员」入口保留（是导航不是编辑）。

桌面端表格的行内编辑不变。

## v1.0.12（修复：插件列表不显示 Logo）

### 原因

`logo.png` 没加进 `build_zip.ps1` 的打包清单——发布包里根本没有这个文件，
装出来的插件目录缺 `logo.png`，AstrBot 插件列表自然不显示图标
（打包脚本是**显式清单制**，v1.0.10 加 Logo 时漏了这一步）。

### 修复

- `logo.png` 加入打包清单；`DEVELOPMENT.md` 的提醒从「新增顶层模块」
  扩展为「新增顶层模块 / 根目录资源」。

> 需要升级到本版并重启 AstrBot（插件目录的文件是安装时落地的）才会生效。

## v1.0.11（修复：WebUI 里 Logo 不显示）

### 原因

Logo 之前作为**独立图片文件**随页面发布（`assets/logo-xxx.png`），而这套沙箱环境对
运行时注入的资源请求有 asset_token 校验——独立图片文件拿不到 token，请求 401，
图片就空了（与当年 CSS 必须内联进 JS 是同一个坑，见 `vite.config.ts` 注释）。

### 修复

- `assetsInlineLimit` 提到 **300KB**：Logo 内联成 data URI 直接进 JS 包，
  不再产生独立图片请求（包体 +165KB，即 Logo 的 base64）；
- `DEVELOPMENT.md` 构建约束补充：新增图片资源须保持在 300KB 内内联。

### 说明

- favicon 指向的 `pages/logo.png` 保留：iframe 里浏览器标签页本就不显示子页面的
  favicon，但直接用 URL 打开控制台时仍然有效。

## v1.0.10（品牌 Logo）

- 新增插件 Logo（`logo.png`），README 顶部展示；
- 控制台顶栏标题旁、移动端导航抽屉里显示 Logo；
- 页面 favicon 指向 Logo。

## v1.0.9（移动端细节优化）

- **总览指标卡**：窄屏从每行 1 张改为 **2 列**（8 张卡 4 行，少滚一半）；
- **移动端筛选面板**：点「搜索」应用条件后**自动收起**（改条件时面板保持展开）；
- 排查记录：路由懒加载在这套部署下没有收益——AstrBot 的 asset_token 重写机制
  要求前端必须单文件产物（`inlineDynamicImports`），已把该约束写入开发者文档；
  图表容器已挂 `ResizeObserver`，窄屏 / 旋转自适应无需额外处理。

## v1.0.8（移动端列表改卡片式 + 筛选收纳）

针对上一版移动端「筛选挤一行、表格靠横向滑动」的问题，按移动端惯例重做私聊 / 群聊列表：

- **卡片式列表（窄屏）**：每个好友 / 群一张卡片——头像 + 名称 + QQ / 群号 + 等级，
  LLM 与指令权限的三态开关直接放在卡片上（不用进详情就能改），
  底部一行今日用量与最后回复；**点卡片进详情抽屉**看完整信息；
- **筛选收纳**：窄屏顶栏只留「筛选」按钮（带生效条件数角标），点开筛选面板
  ——控件纵向铺满、不再挤一行；搜索 / 同步 / 更新头像也在面板里；
- **群卡片**保留「成员」按钮，直达群成员级管控抽屉；
- 分页在窄屏切**简洁模式**（去每页条数选择）；
- 桌面端（>768px）完全不变：表格 + 常驻工具条 + 批量操作照旧
  （批量勾选是表格能力，移动端卡片暂不支持，批量操作请在电脑上进行）。

## v1.0.7（移动端 / 响应式适配）

- **导航**：窄屏（≤768px）时侧边栏收进**抽屉**——顶栏出现「菜单」按钮，点击弹出，
  切换页面自动关闭；宽屏保持原侧边栏不变；
- **顶栏**：窄屏隐藏版本号与「同步于」标签，避免溢出；
- **内容区**：窄屏边距收窄（18px 20px → 12px），给内容留更多宽度；
- **表格**：统计页明细 / 审计、总览 Top 榜、限额两张表、指令例外规则、
  详情近期流水全部补上横向滚动（窄屏可左右滑动查看完整列）；
- **弹窗 / 抽屉**：所有固定宽度弹窗（导出内容、等级编辑、专属额度、群成员设额度）
  加 `max-width: 94vw`；详情抽屉与群成员抽屉在窄屏占满屏宽（94% / 96%）；
  等级编辑里的模型下拉加 `max-width: 100%`，窄屏不再溢出。

## v1.0.6（修复：「最后回复」停止更新）

### 现象

列表里明明前一秒刚回复过，「最后回复」却停在几十分钟前——**记录本身停更了**
（不是 v1.0.5 修的页面刷新问题）。

### 根因

「最后回复」此前**只有一条写入路径**：patch 在 `AstrMessageEvent.send` 上的钩子。
它一旦失效，记录就从那一刻起停止更新，且失败只打 debug 日志、无任何可见告警。
钩子失效的典型场景：

- **其它插件同样 patch 了 `AstrMessageEvent.send`** 且没有链式调用我们打过的补丁
  （后打的直接覆盖先打的）；
- AstrBot 版本升级后发送路径变化，不再经过 `event.send`。

### 修复（三层防护）

1. **钩子保活自检**：维护循环每分钟检查一次钩子是否还在类上，
   被覆盖就重新挂载并打 **warning**（此前失败只打 debug，完全不可见）；
2. **LLM 回复兜底记账**：用量记账成功后顺带写一条「最后回复」
   （preview = 模型回复文本）。send 钩子正常时两处写同一会话、幂等；
   钩子失效时由这里保证最后回复不断更；
3. **指令回复兜底记账**：指令闸门放行时同样补记（kind=command）。
   记账失败升级为 warning（5 分钟节流）。

> 时间戳乱序保护保持不变：秒级单调，旧段 / 旧兜底不会覆盖更新鲜的记录。

## v1.0.5（列表自动刷新 + 详情延迟显示修正 + 筛选框加宽）

### 1. 私聊 / 群聊列表的「最后回复」不更新

排查结论：列表与详情读的是**同一张表、同一个键**（`bot_message`，
按 `(scope_type, scope_id)`），不存在数据分叉——差异在于**刷新时机**：
列表数据只在进页时加载一次，之后页面开着就不会再动；详情抽屉每次打开都现查。
所以「开着列表页等新消息」时，列表永远停在进页那一刻。

修复：两个列表页在**页面可见时每 30 秒静默刷新**（后台标签页自动跳过），
最后回复 / 今日用量跟着实际对话走；离开页面即销毁定时器。

### 2. 详情里出现「0ms 延迟」

不是异常：M6 起**成功执行的指令也记一条流水**（`kind='command'`），
这类行本来就没有「延迟」概念（`latency_ms` 恒为 0），但详情的近期流水表
把它们渲染成了 `0ms`。修正为显示 `-`（与统计页明细的口径一致）。

### 3. 等级筛选框太短

私聊 / 群聊页的「等级」筛选项文本（`等级名（人数）`）被截断，宽度 140px → 200px。

## v1.0.4（修复：用量记录的「模型」错记成系统默认）

### 问题

等级配置了「专用模型」（模型路由）后，统计里该用户 / 群的调用记录显示的仍是
**系统默认模型**——路由实际生效了（对话确实走了配的模型），只是记录写错了。

### 原因

- 路由是在 `route_model` 里用 `event.set_extra("selected_provider", ...)` **按次下发**的，
  只影响本次请求，不改会话默认；
- 而用量记录读的是 `context.get_using_provider_async(umo)` —— 这是**会话默认提供商**，
  按次下发的路由对它不可见 → 永远记成默认。

### 修复

- `route_model` 下发时把「实际选中的提供商 + 它的模型名」一起存进 `_last_route`
  （原本熔断统计就在用这个结构，现在把模型名也带上）；
- `_record_usage` 优先取 `_last_route` 里**本次真正下发的**提供商 / 模型，
  没经过路由的请求才回落读会话默认；
- 顺序保证：熔断统计（`_update_circuit`）只读不弹、且在用量记录之前执行；
  `route_model` 在每次请求开头清键，不会被上一次的路由结果污染。

> 注意：修复只影响**新产生**的记录，历史记录里错记的模型不会追溯改正。

## v1.0.3（「有用量、无最后回复」的口径说明）

### 排查结论

好友页可能出现「今日用量有 token、最后回复无记录」，这是**两个口径不同**，不是数据丢了：

- **今日用量**按 `sender_id` 聚合 —— 他在**群里**聊的 token 也算到他人头；
- **最后回复**只记**私聊**会话 —— bot 在群里的回复属于全群，记在**群**名下
  （`bot_message` 表按 `(scope_type, scope_id)` 存，群回复没有「给谁看的」归属）。

所以「只在群里聊过、从未私聊」的好友 = 有用量、无私聊回复记录。

### 改动

- 好友页「最后回复」列：空记录且今日用量 > 0 时，显示「无私聊记录」并悬停解释
  上述口径（而不是不明所以的空白）；
- 其余情况维持原样：真正没互动过的显示「无记录」。

## v1.0.2（总览页布局重排 + 对象展示名 / 头像）

### 总览页

- **「用量趋势」独占一块**（不再塞进 Tab）；**今日视图的 x 轴改为「小时:00」**
  （此前按天分桶，一整天只有一个点；跨度 ≤ 48 小时的区间一律按小时分桶）；
- **下方一行左右均分**：
  - 左「模型与拒绝」：卡片内部用 Tab 切换 `模型占比 / 拒绝原因分布`；
  - 右「用量 Top 对象」：**头像 + 名称**（备注 / 昵称 / 群名，查不到回退显示 id）
    + 友好 token（K / M）+ 类型显示中文（好友 / 群聊）。

### 统计页

- 「用量明细」的对象列同样补上**头像与展示名**（发起人 QQ 保留为次行）。

### 实现

- `store.summary()`：趋势分桶粒度按区间跨度自适应（≤48h → 小时桶，label `%H:00`）；
- `store.subject_name_map()`：按 id 批量查展示名（`friend_cache` 备注优先、
  `group_cache` 群名），`/overview` 榜单与 `/usage` 明细行各补一个 `name` /
  `scope_name` 字段——一页数据两次 `IN` 查询，无 N+1；
- 自检：展示名映射（备注优先 / 昵称回退 / 查不到不进结果）+ 小时分桶标签。

## v1.0.1（控制台体验：总览 / 统计页分 Tab + 趋势图坐标修复）

### 界面

- **总览页图表区分成三个 Tab**：`用量趋势` / `模型与拒绝` / `用量 Top 对象`——
  之前四块内容纵向排开要滚很久，指标卡与筛选条保持常驻；
- **统计页分成三个 Tab**：`指令维度`（Top 榜 + 热力图）/ `用量明细` / `操作审计`；
  **操作审计懒加载**（第一次切过去才查询），初始少一次请求；
- 两页图表卡改为 `embedded` 无边框样式，与 Tab 视觉更统一。

### 修复

- **趋势图 y 轴 token 数字显示不全**：坐标轴加紧凑格式化（`1.2M` / `3.5K`）、
  `hideOverlap` 防重叠，grid 改用 `containLabel: true` 自动给标签让位；
  右侧「次数」轴关掉分割线，避免与左轴网格打架。

## v1.0.0（发布评审：修复 + 稳固）

功能层面收口（M0–M11 全部完成），本版是一次**全库代码评审 + 修复**，之后进入维护态。

### 修复（Critical）

- **好友等级的「群聊场景值」inherit 时不回落主值**（`gate._level_effect`）：
  reload 出来的场景值是字符串 `"inherit"`（真值），旧的 `or` 链永远回落不到主值——
  「私聊禁、群里放」会违背「inherit = 跟随主值」的承诺，覆盖几乎所有存量等级
  （新建等级的场景值默认就是 inherit）。修复：非显式 `allow / deny` 一律视为没配。
- **群成员 / 全局的「对象级指令总权限」写进去不生效**（`reload_rules`）：
  `_cmd_master` 只加载了 `user / group` 两类，而档位链里有成员层与全局层——
  成员抽屉里给某成员设「禁止指令」后他在群里照样能用，界面还回显「继承」。
  修复：加载范围扩成 `user / group / member / global`。

### 修复（Required）

- **规则导入改为单事务原子写**（`store.import_rules`）：
  此前 replace 模式先删后导、逐行各自 commit，中途失败会留下
  「旧规则已清空、新规则只进一半」且无法回滚。现在导入期间统一提交一次，
  失败整体 `rollback`。
- **导入校验与 `/policy`、`/quota` 口径对齐**：feature 白名单（含 `command:` 后非空）、
  群成员 scope_id 必须是「群号:QQ」、`global` 不收 LLM 规则、
  坏 `mode` 规整、坏 `reset_at` 归 `None`（字符串写进 INTEGER 列会让重置永不命中）。
  上述形态从「静默入库成死数据」改为跳过并计数。
- **批量写接口先全量校验再落库**（`/policy`、`/quota`）：此前边校验边写，
  中途失败时前几条已进库而内存规则未刷新，SQLite 与闸门快照不一致。
- **`/ping` 的规则统计补上 member 维度**（`effect_members` / `usage_members`）。

### 修复（前端）

- 「对象体检」切换类型时清掉残留的群号选择（切回成员模式不必再切走再切回）；
- 群成员抽屉写规则优先用后端下发的 `scope_id`，不在前端拼「群号:QQ」；
- `CommandMatrixRow` 类型不再错误地继承 `CommandRow`（矩阵行并没有
  `global_effect` / `rules` 字段，避免按类型取到 `undefined`）；
- 「群聊 → 成员」的同步时间改取**该群全量**成员的最大 `updated_at`
  （此前取分页结果的 max，翻页时显示偏旧）。

### 自检

- gate 新增 [16] 指令总权限的成员 / 全局层（成员层只在群聊参与、
  全局 deny 兜底、全局 allow ≠ 白名单通行证）；
  「场景值 = 字符串 inherit → 回落主值」的回归用例（指令 + LLM 各一条）；
- store 的导入坏行用例扩充：member 格式 / global+llm / 空 command 名 /
  未知 feature 跳过、坏 mode 规整、坏 reset_at 归 None。

## v0.12.0（M11：指令 × 对象矩阵 —— 「对象体检」）

### 背景

PRD 待办里的最后一项：指令 × 对象的矩阵视图。此前想回答
「**这个人到底能用哪些指令**」，只能去「指令」页逐条点开规则、或在脑子里推演档位链；
矩阵直接把结论摊平。

### 新增

- **「指令」页新增「对象体检」卡**：
  - 选一个对象（**好友 / 群 / 群成员 / 等级 / 全局**，均可搜索下拉），列出
    **每条指令**的结论：`可以用 / 被拦下` + **结论来自哪一层**
    （好友专属 / 群等级 / 这条指令的全局策略 / 系统默认…）；
  - 顶部汇总：共 N 条指令，可用 X / 被拦 Y；
  - 「该对象的例外」列：可**就地**给这个对象设这条指令的放行 / 禁止 / 继承
    （即写 `command:<指令名>` 的单条规则，好友模式还区分私聊 / 群聊场景）；
  - 选**等级**时给出行内说明：等级只管「整体能不能用指令」（在限额 → 等级管理配），
    单条指令的例外要配到好友 / 群 / 成员上——想给这一档里个别人开白名单，
    去「私聊 / 群聊」页设那个人的指令权限。

### 实现

- 后端 `Plugin.command_matrix_of(scope_type, scope_id, scene)`：
  - 好友 / 群 / 群成员 / 全局 → 复用 `_probe_subject()` 构造探针对象跑档位链；
  - **等级** → 用 `dataclasses.replace` 派生一份临时 Rules 快照，把一个探针对象
    临时归到该等级，再跑链——不碰热路径正在用的那份快照；
  - 每条指令跑 `gate.check_command()`（与实际拦截**同一份判定内核**，结论与线上一致）；
- 新接口 `GET /commands/matrix?scope_type=&scope_id=&scene=`；
- `PolicyItem.scope_type` 前端类型补上 `member`。

### 说明

- 判定逻辑全部复用既有档位链（M5–M8），无行为变化，`tests/test_gate.py` 全绿。

## v0.11.0（M10：规则导出 / 导入）

换机器、重装插件、想备份一套复杂的权限配置 —— 以前只能整库拷贝 `.db`，
现在可以在「配置 → 备份与迁移」里导出 / 导入**规则**。

### 导出什么（以及刻意不导出什么）

导出四类**规则**：权限规则（含场景与群成员维度）、等级（含两套默认权限与模型路由）、
对象归级、额度（含 `member` 维度）。

**不导出**用量计数、明细、审计、群成员缓存、最后消息：它们是历史数据，
跟着规则搬没意义，还会让文件很大（要连历史一起备份，直接备份 `.db` 文件）。
文件带 `meta`：插件版本、**结构版本**、导出时间与各类条数。

### 导入策略

- **合并（merge，默认）**：只覆盖文件里出现的条目，本地其它规则保留；
- **覆盖（replace）**：先清空这四类规则再导入（用量与日志不动），前端会二次确认。

### 关键实现：等级 id 必须重映射

`quota_level.id` 是自增主键，换台机器导入必然与本地 id 撞车。所以导入时
**按 `(kind, name)` 找到/新建等级**拿到新 id，再把「对象归级」与「等级额度」
里引用的旧 id 改写成新 id。少了这一步，导入后归级和等级额度会全部指向别人的等级，
**而且不会报错** —— 这是导入功能最容易埋的雷，自检专门盯住了它。

其它容错设计：

- **坏行跳过而不是整次失败**：非法 `period` / `scene` / `scope_type` 计入 `skipped`，
  未知的 `effect` 规整成 `inherit`。导入是「尽量恢复」，一条坏数据让整批回滚更糟；
- 来自**更新结构版本**的备份直接拒绝（提示先升级插件），避免悄悄丢字段；
- 导入后立即 `reload_rules()`，闸门马上用上新配置；并写一条导入审计。

### 接口与界面

- `GET /rules/export` → `{filename, content, meta}`；`POST /rules/import` → 统计；
- 「配置」页新增「备份与迁移」卡片：导出（下载 + 弹窗可复制）、
  粘贴或**选择本地 .json 文件**（FileReader，沙箱里也能用）、模式选择、导入结果统计。

### 自检

`test_store` 新增 [16] 规则导出 / 导入：导出条数、**归级与等级额度指向重映射后的新 id**、
两套默认权限与模型路由、场景规则、群成员维度规则与额度、全局额度、
坏行跳过（`skipped=2`）、未知 effect 规整、`replace` 清掉本地多余等级。

## v0.10.0（M9：按成员的额度）

M8 只做了成员级的**权限**，用量是只读展示；这次把额度也补上：
「某人在**这个群**里每天 500 token」这种限额现在可以配了。

### 关键设计：第三个记账维度

额度的关键不是「能不能配上限」，而是**用哪个用量去比**。成员级必须用
「他在**这个群**里的量」，否则「群里限 500 却按跨群合计算」形同虚设。

所以 `usage_counter` 多了一个维度 `scope_type='member'`（键同为 `群号:QQ`）：

```
每次 LLM 调用 + 消费一起记三套用量：user（跨群合计）/ group（群）/ member（群里的这个人）
```

- 只对**群聊**多记 member 一套（私聊没有「群成员」概念），代价是每次调用多 3 行 upsert，
  与既有「用量必须无条件记账」的原则一致（配额度之前的历史消费也留着，不会凭空从 0 开始）；
- `Gate.layers_for` 里 member 档位的 `usage_type` 改成 member ——
  权限链不受影响（权限只看 `scope_id`），额度链从此按群内用量判定；
- **表结构不用改**：`llm_quota` / `usage_counter` 本来就按 `scope_type` 分键，
  所以 v0.10.0 **没有 schema 变更**（仍是 v7）。

### 能力

- 群成员抽屉新增「额度（本群）」列：显示 已用/上限 + 进度条 + **生效档位**
  （成员专属 / 好友等级 / 群 / 全局 —— 与闸门同源，不会两套算法）；
- 点「设额度」打开弹窗：日 / 月 / 累计三个周期，**留空 = 不修改**、
  **填 0 = 明确不限（占住档位）**、填数字 = 设上限；另有「清除该周期额度」与
  「清零本群用量」；勾选多人后可「批量额度」；
- 「限额」页的「对象专属额度」列表纳入成员额度（`scope_id` 会显示成「群 88888 的成员 10001」），
  新增/编辑弹窗也支持「群成员」类型（填 `群号:QQ`）。

### 顺手修掉的隐患

`main._effective_limit()`（额度预警文案用）原来**手写了一份档位链副本**，
加了「群成员」这一层后极容易与闸门漂移；现在改为直接复用 `gate.layers_for()`
的链，并新增 `_usage_table()` 统一三个维度的内存计数表。

### 自检

- `test_gate` 新增 [17] 按成员的额度：未超限/超限判定、只对该群生效、不影响同群他人、
  成员额度优先于好友与群额度（各层用量互不干扰）、私聊里没有成员层、
  member 层取群内用量而非跨群合计、成员层配了额度后不再回落群层；
- `test_store` 新增 [15]：member 维度用量累加 / `reset_at` 只在首次写入 /
  月周期同记 / 成员额度读写 / 单周期清零不影响其它周期。

## v0.9.0（M8：群成员级管控）

### 能力

- **群聊页每行新增「成员」按钮**（带已缓存人数），打开「群成员」抽屉：
  列出该群成员（头像 / 群名片 / 昵称 / 身份：群主 / 管理员 / 成员 / QQ / 本群今日用量），
  可搜索、分页、勾选后**批量**设权限。
- 每个成员可单独设 **LLM 权限**与**指令权限**（三态：继承 / 放行 / 禁止）。
- 右上角「同步成员」从协议端拉取成员列表（``get_group_member_list``）；
  本地缓存后离线也能看，没同步过的群给空态引导。
- 新增配置 ``sync_group_members``（默认 **false**）：打开后每次自动同步会顺带拉取所有群的成员。
  **默认关闭**，因为量级是「群数 × 人数」，很吃协议端；按需在单个群手动同步更合适。

### 权限模型

新增最具体的档位 **群成员专属**（``scope_type='member'``，键是 ``"群号:QQ"``）：

```
群聊：群成员专属 → 好友专属 → 好友等级 → 群专属 → 群等级 → 全局默认
私聊：（不存在这一层）
```

- 它限定「这个人 + 这个群」，范围最窄，所以**优先级最高**：
  典型用法是「**整群禁止，只给某个人放行**」（以前做不到 —— 好友级放行会被群级禁止的
  语义绕开，而成员级正好插在最前面）；
- 它**只影响这个群**：不会带到别的群，也不影响私聊；跨群的统一设置仍去「私聊」页
  （切到「群聊」场景）配；
- 指令权限同样支持（对象级总权限与单条指令的规则都能用 ``member`` scope）。

### 存储与实现

- 结构 v6 → v7：新增 ``group_member`` 表（平台 / 群 / QQ / 昵称 / 群名片 / 身份 / 加群时间），
  覆盖式写入（退群的人从缓存消失，但**不动 policy 表** —— 规则留着，回来即生效）；
- ``store`` 新增：``upsert_group_members`` / ``list_group_members``（群主→管理员→成员排序、
  按 QQ / 昵称 / 群名片搜索）/ ``group_member_counts`` / ``get_group_member`` /
  ``usage_sums_in_group``（**只统计该群内**按发言人聚合的用量 —— 回答「他在这个群用了多少」）；
- ``sync`` 新增 ``sync_group_members()``（``_call`` 支持透传参数）；
- 新接口：``GET /group/members``（读缓存，不打协议端）、``POST /group/members/sync``；
  ``/policy`` 支持 ``scope_type='member'``（校验 ``scope_id`` 必须是「群号:QQ」）；
  ``/groups`` 每行返回 ``member_cached``（已缓存成员数）。

### 自检

- ``test_gate`` 新增 [16] 群成员级管控：成员层禁止 / 只对一个群生效 / 私聊不受影响 /
  **整群禁止 + 该成员放行** / 压过好友与等级 / 指令权限同样支持；
  并更新 [6] 的档位链顺序断言（群聊链现在以 ``member`` 开头）；
- ``test_store`` 新增 [14]：成员缓存覆盖式写入与排序（群主→管理员→成员）、按昵称 / QQ 搜索、
  退群的人消失、每群成员数、**群内按发言人聚合用量** 校验。

## v0.8.0（M7：权限加「场景」维度 —— 私聊与群聊分开管）

### 要解决的问题

「好友专属」与「好友等级」这两层在**私聊和群聊里都会参与判定**，所以以前
「私聊禁掉某人」等于「他在所有群里也不能用」。现在把这一层拆成两个维度：

> 私聊禁用某人 → 他私聊用不了；群聊没禁用 → 他在群里照用。

### 规则

新增 `scene` 维度，取值 `private` / `group` / `''`（通用）：

```
取某场景的规则：先看「场景专属」，没有再回落到「通用」
  私聊：好友专属(私聊) → 好友专属(通用) → 好友等级(私聊) → 全局默认
  群聊：好友专属(群聊) → 好友专属(通用) → 好友等级(群聊→回落私聊) → 群专属 → 群等级 → 全局默认
```

- **旧数据全是「通用」**（`scene=''`），两个场景都生效 —— **升级不改变任何既有行为**，
  不需要迁移数据，也不会突然放行/拦截；
- 等级同理：好友等级多了「群聊默认权限」（LLM 与指令各一套），
  默认 `inherit` = 跟随私聊那一套（同样是旧数据的语义）；
- 指令权限（对象级总权限）同样分场景；单条指令的规则也支持（判定已按场景回落），
  但控制台暂不放场景选择器，写入的仍是「通用」规则。

### 改动

- **存储 v6**：`policy` 表加 `scene` 列 —— 唯一键要变成
  `(scope_type, scope_id, feature, scene)`，SQLite 改不了唯一约束，所以**重建表**
  （旧行一律填 `''`）；`quota_level` 加 `effect_group` / `command_effect_group` 两列。
- **判定内核**：抽出口径统一的 `effect_in_scene()`（场景专属 → 通用），
  LLM 权限、对象级指令权限、单条指令三处共用；`layers_for` 给每个档位带上会话场景。
- **接口**：`/policy` 支持 `scene`；`/friends`、`/subject` 支持 `scene`
  （权限列与抽屉按当前场景取值）；`/levels` 返回并接收两套群聊默认权限。
- **前端**：
  - 私聊页顶部加「私聊 / 群聊」场景切换，权限列标题与批量下拉都跟着变
    （写明「这次改的是哪一套」）；
  - 详情抽屉加同样的切换（群详情没有这个维度，天然只属于群聊场景）；
  - 等级编辑器：好友等级多出「群聊默认权限」（LLM / 指令各一行，可「跟随私聊」）；
    等级列表在配了群聊那套时多标一行「群聊：…」；
  - 「指令」页的例外规则会标出「仅私聊 / 仅群聊」；
  - 权限说明卡补上场景维度的解释。

### 自检

- `tests/test_gate.py` 新增 [15] 场景维度：只禁私聊 / 只禁群聊 / 通用规则两个场景都生效 /
  场景专属覆盖通用 / 等级主值与群聊值 / 群等级不受影响 / 指令与单条指令同样分场景；
- `tests/test_store.py` 新增 [12.1] v5 → v6 迁移：旧规则落到「通用」、等级新列默认 `inherit`、
  重建后唯一键含 `scene`（同对象可同时存「私聊」与「通用」两条）、重开不重复迁移。

## v0.7.1（列表体验：等级筛选 / 默认按最后回复倒序 / 等级数量）

三处列表体验调整，私聊与群聊两页一致：

- **默认排序改为「最近回复」倒序**：进页面先看「谁最近还在用」，从没回复过的对象沉底。
  排序下拉里「最近回复」提到第一个（后端 `sort` 默认值也一并改成 `last`）。
- **新增「等级」筛选**：可选「全部等级 / 未分组 / 某个等级」，
  与已有的「LLM 权限 / 指令权限」筛选叠加生效。
  后端 `/friends`、`/groups` 新增 `level_id` 参数（空 = 全部，`0` = 未分组，`>0` = 等级 id）。
- **等级数量只在「下拉」里显示，且实时刷新**：
  - 等级管理页表格去掉「成员数」列；
  - 私聊 / 群聊页的「等级筛选」下拉与「等级」列下拉继续带数量 `名称（N）`；
  - 每次刷新列表时顺带重新拉一次等级，所以在等级管理页改完归级回到列表就是新数量
    （之前只在进页面时取一次，数量会停在旧值）。

## v0.7.0（M6：统计页 —— 明细 / 指令维度 / 审计 / 导出）

### 背景

后端的 `/usage`（明细）与 `/audit`（审计）从 M0 就在，但**前端一直没有页面用它们**，
PRD §4.4 要求的「明细 + CSV 导出」以及 M3 追加的「最常触发 / 被拒指令 Top10、时段热力图」
都还空着。本次补齐，新增「统计」页。

### 新增

- **统计页（`#/stats`）**
  - 用量明细：区间（今日 / 7 日 / 30 日）+ 类型（LLM / 指令）+ 状态（正常 / 被拒）+
    对象 ID + 发起人筛选，分页，token 分项与「估算」标记；
  - **CSV 导出**：后端生成（带 UTF-8 BOM，Excel 不乱码），前端 Blob 落地；
    沙箱可能拦下载，所以同时弹出内容供复制；
  - **最常触发的指令 / 最常被拦的指令 Top10**；
  - **LLM 活跃时段热力图**（24 小时 × 星期，按本地时间分桶）；
  - **操作审计**（谁在什么时候改了什么规则，payload 可悬停查看）。
- **指令触发流水**：新增配置 `track_command_usage`（默认开），把成功执行的指令也记一条
  `kind='command'` 的流水，用于「最常触发的指令」。它与 LLM 口径完全分开
  （`calls` / token 只统计 `kind='llm'`，已加自检固定这一点）；关掉后统计页只会看到被拦记录。
- 后端：`store.top_commands()` / `store.hour_heatmap()` / `store.export_usage_csv()`；
  新接口 `/stats/commands`、`/usage/export`。
- 私聊 / 群聊页支持按**指令权限**过滤；两页顶部新增「权限怎么算」说明卡
  （讲清「继承 / 放行 / 禁止」是**互斥三态**，以及 LLM 与指令是两套独立规则）。

### 修正

- 「指令」页的说明还写着「指令权限不参与等级档位」——M5 之后已不成立，改正为
  「等级只管整体能不能用指令，且压得住单条指令的放行」。
- 群聊页挂着「群成员级管控将在 M3 上线」的过时标签（该功能**实际未实现**），移除；
  README 里把它从「M3 已完成」改为「待办」。

### 自检

- `tests/test_store.py` 新增 [5.1]：指令 Top 榜（含排序）、热力图分桶、
  CSV（BOM / 表头 / 行数 / 按类型过滤）、以及「指令流水不污染 LLM 口径」。

## v0.6.0（M5：指令权限补齐「对象 / 等级」维度）

v0.5.0 的指令权限只能**按指令名**配（一级一条规则），少了 LLM 权限那样的「对象维度」：
没法一次性说「这个等级 / 这个群不能用任何指令」。本次补齐，两个维度并存。

### 能力

- **私聊 / 群聊页新增「指令权限」列**：三态（继承 / 放行 / 禁止），
  与「LLM 权限」并排、但完全独立；批量操作下拉也分成「LLM 权限 / 指令权限」两组。
- **等级新增「默认指令权限」（三态）**：设为**禁止** → 该等级下的好友 / 群
  **不能用任何指令**；等级列表的「默认权限」列同时展示 LLM 与指令两套结论。
- 详情抽屉的权限卡拆成两行（LLM / 指令），并显示指令权限的**生效来源**
  （好友专属 / 好友等级 / … / 系统默认）。
- 「指令」页顶部说明补上了「对象维度在哪配」，避免只看到单条指令规则。

### 判定：两段式（`gate.check_command`）

```
第一段 · 对象级总权限：好友专属 → 好友等级 → 群专属 → 群等级 → 全局
        —— 「这个人 / 这个群能不能用指令」；命中 deny 直接拦
第二段 · 该条指令的规则：好友专属 → 群专属 → 这条指令的全局开关
        —— 「这条指令能不能用」；都没有则用配置「指令默认策略」兜底
```

- 对象级 `deny` 是**硬拦截**，但更具体的层可以覆盖：把整个等级设为禁止，
  再给某个人设「放行」，就能实现「整档禁止 + 白名单」；
- 单条指令的规则**不带等级层**（等级只管整体开关，不管某一条指令），
  所以「等级禁止」压得住单条指令的「放行」，反之不行（要在对象级开白名单）；
- 兜底仍是与 LLM 完全独立的 `default_command_effect`。

### 存储与实现

- 复用 `policy` 表：对象级指令权限的 feature 键是 **`command`**（单条指令仍是
  `command:<指令名>`，前缀不重叠、互不干扰），**无需新建表**；
- 结构 v4 → v5：`quota_level` 新增 `command_effect` 列（`ALTER TABLE`，可重入）；
- `gate.py` 抽出通用档位链解析 `_first_effect()`，LLM 权限与指令总权限共用同一条链
  （行为不变，原有用例全绿）；
- `/policy` 放宽 feature 校验（`llm` / `command` / `command:<指令名>`）；
  `/subject` 返回 `command_master`（显式值 + 生效层）；`/levels` 返回 `effect_command`。
- 自检新增 [14]：对象级指令权限（群禁止 → 整群拦截、好友放行 → 白名单、
  等级禁止 → 整档拦截、对象级 deny 优先于单条指令、对象级放行仍受单条指令约束）；
  store 侧覆盖 `command_effect` 读写与 v5 迁移。

## v0.5.0（M4：指令权限 —— 按指令名配置禁用 / 放行）

之前只有 LLM 权限，指令权限一直没落地（PRD 里排到 M3）。本次补齐。

### 能力

- 控制台新增 **「指令」页**：自动列出 AstrBot 里**所有已注册的指令**
  （带说明、别名、来源插件、指令组标记），每条指令一份独立规则。
- 每条指令有：

  - **全局策略**（三态：继承 / 放行 / 禁止）——一键「全局禁用这条指令」；
  - **例外规则**：针对某好友 / 某群单独放行或禁止（用于「全局禁止 + 个别白名单」这类场景）。

- 解析顺序：**好友专属 → 群专属 → 该指令的全局策略 → 配置里的「指令默认策略」**，
  **命中最具体的一层即止**。
- 「指令默认策略」可设为 `deny` 变成**白名单模式**（未显式放行的对象用不了任何指令）；
  它与 LLM 的「全局默认策略」**相互独立**，把 LLM 设成白名单不会顺带关掉所有指令。
- 被拦截时给用户发提示（文案可配，支持 `{command}` 占位），管理员豁免沿用配置。

### 拦截机制（AstrBot 4.27）

- 本插件注册一个 **「所有消息 + 高优先级」** 的处理器
  （`@filter.event_message_type(ALL, priority=command_guard_priority)`）。
  waking 阶段已把本次命中的 handler 写进 `activated_handlers`
  （`waking_check/stage.py:253`），而 process 阶段**按优先级降序**依次执行这些 handler、
  并在每个 handler 前检查 `event.is_stopped()`（`star_request.py:36-38`）——
  所以在这个处理器里 `stop_event()` 就能让后面的指令 handler 不执行（也不会进 LLM）。
- 指令名取 `get_complete_command_names()[0]`（完整主名，子指令形如 `父 子`），
  **别名与主名共用一条规则**（不单独建键）。
- 指令权限**不参与等级档位**：等级是「模型 + 额度 + LLM 权限」的概念，
  让「等级 = 禁止」顺带禁掉指令会让两件事莫名牵连。
- 存储复用 `policy` 表：`feature = 'command:<指令名>'`（LLM 仍是 `'llm'`），
  `scope_type` 扩展出 `global`（`scope_id='*'`），**无需改表结构**。
- 拦截流水记 `kind='command'`，不污染 LLM 的调用统计口径。
- 指令被卸载/改名后残留的规则可在页面上「清理失效规则」。

### 其它

- 配置页新增「指令权限」分区；`/commands`、`/commands/prune` 接口；
  `/policy` 支持 `feature` 参数与 `scope_type=global`（LLM 的全局默认仍走配置项，不允许写库）。
- 自检扩充：[13] 指令权限判定链（好友/群/全局/默认、白名单模式、与 LLM 默认互不影响）、
  store 侧的指令 feature 读写与失效规则清理。

## v0.4.1（修正模型选择：两个可搜索下拉，去掉多余的「模型名」）

v0.4.0 的等级模型选择把「提供商」和「模型名」拆成两个输入框，配置含混、也不符合
AstrBot 的模型口径。本次按 model_panel 的做法重做：

- **模型以「提供商」为单位**：AstrBot 里一个模型提供商就绑定一个模型，
  所以下拉里**一项 = 一个模型**，显示为「供应商 · 模型」
  （供应商名取 `provider_source_id` / `name`，不用 `openai_chat_completion` 这类 type）。
- **只留两个下拉**：主模型、备用模型；都支持**输入筛选**（filterable）、可清空。
  当前默认的模型会标注「当前默认」，处于熔断的会标注「熔断中」。
- 等级编辑器里不再有单独的「模型名」输入框；`selected_model` 也不再下发
  （换提供商即换模型，AstrBot 会用该提供商自己的模型）。
- 存储结构 v3 → v4：去掉 `quota_level.model` 列（重建表迁移，老数据保留）。
- `/providers` 增加 `name` / `label` / `is_default` 字段；等级列表的「模型」列直接用
  「供应商 · 模型」展示主/备模型。自检同步更新（gate 的路由/降级用例、store 的 v2→v4 迁移）。

## v0.4.0（M3：等级模型路由 —— 主模型 / 备用模型）

### 能力

- **等级可指定模型**：主模型（提供商）+ 可选模型名 + 备用模型（提供商）。
  属于该等级的对象，每次 LLM 请求前都会被下发到指定提供商。
- **路由语义**：**私聊按「好友等级」、群聊按「群等级」**。
  有意与权限/额度的档位链不同 —— 群聊的上下文与计费都属于「群」（同一个 umo），
  若按发言人切模型会让同一个群的上下文串味；所以群聊里**不会**回落到发言人的好友等级。
- **主模型不可用 → 自动走备用**：两种情况都会落过去
  ① 主提供商当前未加载（被 disabled / 删了）；
  ② 主提供商连续失败达到阈值触发**熔断**（默认连续 2 次，熔断 300s，到点自动恢复）。
  插件只会统计**自己下发过**的提供商，不碰 AstrBot 的默认模型。
- 提供商列表由后端 `/providers` 提供（只列**已加载**的），前端只能从名单里选。

### 实现依据（AstrBot 4.27，全部走官方通道，没有改框架内部）

- 下发布点是 `@filter.on_waiting_llm_request()`（`internal.py:214`）。它早于
  `_select_provider()`（`:230`，读 `event.get_extra("selected_provider")`）与 `req` 构造
  （`:238`，读 `selected_model` → `req.model`），所以来得及生效；
  而 `on_llm_request`（`:333`）在提供商已选定之后才触发，那里只能再兜一次 `req.model`。
- **绝不设未知的提供商 id**：`astr_main_agent.py:242` 遇到不存在的 id 会直接放弃本次请求并报错。
  插件先做「已加载 + 未熔断」校验，都不满足就**不干预**（走 AstrBot 默认模型）。
- 模型名只作用于主提供商；落到备用时用备用自己的默认模型
  （与 AstrBot 自己切提供商时的处理一致，`astr_main_agent.py:1684`）。
- AstrBot 内置的 `fallback_provider_ids` 是**全局**配置，且在 `MainAgentBuildConfig`
  构造时就固定（`internal.py:143`），插件无法按会话注入 —— 所以等级级的主/备切换由
  本插件的「选择阶段 + 熔断」实现。把等级用到的备用提供商也加进 AstrBot 全局
  「备用提供商」列表，还能额外获得框架级的运行时兜底。

### 其它

- 控制台：新增 `/providers` 接口；「限额 → 等级管理」里可直接选主/备模型与模型名；
  等级列表新增「模型」列；详情抽屉显示「生效模型」；配置页新增「模型路由」分区
  （总开关 / 熔断阈值 / 熔断时长）。
- 存储结构 v2 → v3：`quota_level` 新增 `provider_id` / `model` / `fallback_provider_id`
  三列（自动 `ALTER TABLE` 迁移，可重入）。

## v0.3.0（M2：自定义等级与三层限额、头像后端缓存、bot 最后回复、权限三态控件）

### 1. 自定义等级 + 三层限额（存储结构 v2）

- 新增「等级」：好友与群**各一套**，可自定义名称 / 排序 / 说明；等级可带
  **默认 LLM 权限**（继承 / 放行 / 禁止）与**额度模板**（日 / 月 / 累计 + 拦截 / 观察）。
- 好友 / 群可逐个或批量归级（列表页直接下拉选等级）。
- 限额变成三层结构：**全局默认 → 等级模板 → 对象专属**，解析规则：
  `好友专属 → 好友等级 → 群专属 → 群等级 → 全局`，**命中最具体的一层即止**。
  这样「VIP 等级 50 万」不会被「全局 5 万」反手拦掉，等级才能真正当档位用
  （旧版是「用户额度与群额度都参与判定」，语义已按新模型收紧，详见 README）。
- **额度与用量分离**（schema v1 → v2，自动迁移）：`llm_quota` 只存上限，
  已用量移到独立的 `usage_counter`；换额度模板不再清零用量。
  用量现在**无条件记账**（即使对象没配额度）——等级 / 全局额度是模板，
  判定必须用对象自己的用量。
- `limit_tokens=0` 的语义定为**「明确不限」并占住档位**（更粗的层不再参与）；
  要恢复继承需显式删除该周期（`limit_tokens=null` / `delete=true`）。
  否则「VIP 等级 = 不限」永远会被全局额度先拦掉。

### 2. bot 最后回复（时间 + 类型）

- 私聊 / 群聊列表新增「最后回复」列：bot 在该会话**最后一条消息的时间**与类型
  （**LLM 回复 / 指令回复 / 普通消息**），悬浮可看预览与指令名；可按「最近回复」排序。
- 类型判定不猜文本：LLM 由本插件的 `on_llm_request` 钩子给事件打标；
  指令取 waking 阶段写进 `activated_handlers` 的指令过滤器；其余归为普通消息。
- 记录点是对 `AstrMessageEvent.send` 的补丁（幂等安装、terminate 还原），
  而不是官方的 `after_message_sent` 钩子——后者在流式回复、空链、插件直发等
  多条路径上不会触发，覆盖不全。新配置项 `track_bot_messages` 可关。

### 3. 头像：后端抓取 + 缓存 + 更新

- 新增 `avatar.py`：好友头像与**群头像**都由后端从腾讯 CDN 抓取并缓存到
  `data/plugin_data/astrbot_plugin_user_gateway/avatars/`，按 `avatar_cache_days`
  （默认 30 天）过期重取；抓取失败自动用过期缓存兜底（离线可用）。
- 前端批量按当前页的 id 拉取（data URI，带在途去重），三级兜底：
  后端缓存 → 腾讯 CDN → 首字母色块。
- 列表页新增「更新头像」按钮（强制重取当前页）；配置项 `avatar_source`
  （`backend` 默认 / `cdn` 旧行为 / `off`）、`avatar_cache_days`、`avatar_timeout_sec`。

### 4. 权限操作改为三态分段控件

- 「放行 / 禁止」两个并排按钮容易让人以为是两个可同时打开的开关。
  实际语义是**同一个开关的三个互斥状态**（继承 / 放行 / 禁止），
  现改为一个分段控件并给每个状态配了说明 tooltip；批量操作同步改为
  「批量权限」下拉（放行 / 禁止 / 恢复继承）。

### 5. 其它

- 控制台接口新增：`/levels`、`/levels/delete`、`/subject-level`、`/avatars`、
  `/avatars/refresh`；`/friends` `/groups` 支持服务端权限过滤与「最近回复 / 等级」排序；
  `/subject` 返回等级、额度档位链与最后回复；总览新增 bot 回复类型分布。
- 详情抽屉新增「额度档位链」：逐层显示 好友专属 / 等级 / 群 / 全局 的配置与用量，
  并标出「生效」「已超限」的那一层 —— 界面上能直接回答「这个人到底按哪条额度在算」。
- 自检脚本扩充：`tests/test_gate.py`（档位链与三层额度解析）、
  `tests/test_store.py`（等级 / 归级 / 用量计数 / 最后消息 / **v1→v2 迁移**）。

### 升级注意

- 数据库自动从 v1 迁移到 v2（首次加载时执行，已用量会平移进用量计数表）。
- 若之前用 `load_avatars=false`，现在请把 `avatar_source` 设为 `off`（两者都会关闭头像）。

## v0.2.1（修复：v0.2.0 的安装包缺文件，导致插件加载即报 No module named 'quota'）

现象：装上 v0.2.0 后插件报 `No module named 'quota'`，无法加载。

根因（**发布包缺文件**，不是代码问题）：`build_zip.ps1` 用的是**显式包含清单** `$includeList`，
v0.2.0 新增了三个顶层模块（`gate.py` / `quota.py` / `sync.py`）却忘了加进清单，
于是打出的 zip 里只有 `main.py` / `store.py` / `webui_api.py`。
`main.py` 的相对导入 `from . import quota` 因此失败，而它的 `except ImportError` 兜底
（为「本地直接跑 main.py」准备的平铺导入）又必然再失败一次，
最终把真因掩盖成了看起来像代码问题的 `No module named 'quota'`。

修复：

- `build_zip.ps1`：补进 `gate.py` / `quota.py` / `sync.py`；并新增**打包前自检** ——
  仓库里任何顶层 `.py` 没进 `$includeList` 就直接报错退出，杜绝同类漏包
  （这类问题静默于构建期、暴露于安装期、且错误信息误导，必须让它早期失败）。
- `main.py`：导入段不再吞掉真因。插件目录先加进 `sys.path`（本地调试用），
  相对导入失败时若平铺导入也失败，则抛出同时包含两个原始错误的异常，
  并明确提示「若是 No module named 'gate'/'quota'/'sync' 则说明安装包少了文件」。

## v0.2.0（M1：LLM 权限内核、好友群同步、真实数据）

需求与设计见工作区 `docs/astrbot_plugin_user_gateway_PRD.md`。

### 新增：LLM 闸门（本版核心）

- `gate.py`：**纯逻辑判定内核**（零 IO、不依赖 AstrBot，便于单测）
  - 三级作用域：用户级 > 群级 > 全局默认，效果 `allow` / `deny` / `inherit`
  - 管理员豁免（`admins_id`，可关）
  - 额度判定：按「对象 × 周期」逐条比对，多周期全部生效、命中任一即超限
    （优先报告最紧凑的周期）；`mode=observe` 只记录不拦截
  - `Cooldown`：同一对象 N 秒内只提示一次，避免连续被拒时刷屏
- `main.py` 接入 `@filter.on_llm_request()`：拒绝时**直发提示** + `event.stop_event()`
  掐断本次 LLM 调用（依据 agent 阶段 `if await call_event_hook(...): return`）。
  选它而不是消息级硬拦，因为**只影响 LLM** —— 指令与其它插件的 handler 完全不受影响。
- 判定全部走内存缓存（策略写入后 `reload_rules()` 重建），热路径零 SQL；
  任何异常一律**放行**（fail-open），绝不吞消息。
- `guard_priority` 配置真的生效：`initialize()` 里改写已注册钩子的
  `extras_configs["priority"]` 并原地重排注册表（用于「被别的插件抢先 stop」时抢回位置）。

### 新增：token 记账与额度扣减

- `@filter.on_llm_response()` 记录每次 agent 结束的用量（输入 / 缓存 / 输出、模型、供应商、
  延迟），并按内存额度缓存累加已用量（一条 SQL 覆盖该对象所有周期）。
- provider 不返回 usage 时按字符数粗估并标 `estimated=1`（控制台区分展示，不与真实用量混淆）。
- 额度达 `warn_ratio` 时给管理员发**一次**预警（只在跨过阈值那一刻报）。
- 额度命中时给用户提示 + （可选）通知管理员；被拒事件写流水（`status=denied` + 原因）。
- 后台维护任务每分钟扫描到期的日/月额度并清零、重建内存缓存。
- **已知粒度偏差（计划 M2 处理）**：`on_llm_response` 只在「最终无工具调用的那一轮」触发
  （`tool_loop_agent_runner.py:912`），多轮工具循环的中间轮 token 不计入 —— 对限额是系统性低估。
  M2 会改为按轮累计，或增量读取 AstrBot 自带的 `provider_stats` 表。

### 新增：好友 / 群同步

- `sync.py`：枚举所有 aiocqhttp 平台实例 → `get_client().call_action("get_friend_list" /
  "get_group_list")` → **覆盖式**写入缓存；逐平台收集成败原因，绝不向上抛
  （一个平台失败不影响另一个，也不影响插件其它功能）。
- 后台按 `sync_interval_min` 定时同步（启动后先等 30 秒，避开协议端尚未连上的窗口）；
  控制台「立即同步」走同一条路径（内部互斥锁，避免手动与定时同时打协议端）。
- 顶部状态：`同步于 <时间>` / `同步失败`（悬停看具体原因）。

### 控制台

- 私聊 / 群聊页：新增「今日用量」列、**排序**（最近活跃 / 今日用量 / 昵称 / QQ 号，群另有人数）、
  「同步列表」按钮；**点击对象名打开详情抽屉**。
- 新增 `SubjectDrawer` 组件：对象信息 + 权限一键切换 + 各周期额度进度与快捷设置（含清零）+
  近 7 日用量柱线图 + 近期流水表。
- 新增接口 `POST /sync`、`GET /subject`；`/friends`、`/groups` 支持 `sort` 参数；
  `/ping` 增加同步状态与「内存中已加载的规则数量」（便于确认改动是否真的生效）。

### 测试

- 新增 `tests/test_gate.py`：判定内核 44 项断言（三级作用域优先级、管理员豁免、各开关、
  额度多周期与观察模式、提示冷却、周期重置时间、token 口径换算与估算）。
  用法：`uv run --no-project python tests/test_gate.py`
- `tests/test_store.py` 扩展到覆盖新 DAO：`usage_sums`（按维度聚合 + 非白名单列拒绝）、
  `subject_stats`、`get_friend` / `get_group`、`list_quotas_of`、
  `add_used` 省略周期时全周期累加、`reset_used` 带 `reset_at`。

## v0.1.1（修复：控制台页面在 AstrBot 里直接崩白屏 —— sandbox iframe 与桥接信封）

现象：装好后打开「萌萌权限控制台」页面，控制台报
`SecurityError: Failed to read the 'localStorage' property from 'Window': The document is sandboxed and lacks the 'allow-same-origin' flag`，页面白屏。

根因（两个都属于「AstrBot 插件 Page 的运行环境约束」，之前没有查证就按普通网页写了）：

1. **插件页跑在没有 `allow-same-origin` 的 sandbox iframe 里**。
   AstrBot 的 iframe 属性是 `sandbox="allow-scripts allow-forms allow-downloads"`
   （`dashboard/src/views/PluginPagePage.vue`）。缺少 `allow-same-origin` 意味着
   `localStorage` / `sessionStorage` / `document.cookie` 一访问就抛 `SecurityError`。
   而本插件在 `App.vue` 的 setup 顶层直接读了 `localStorage`（读主题），
   异常发生在 `app.mount()` 期间 → 整个页面直接崩掉。
2. **桥接返回信封用错了格式**。Dashboard 的处理是
   `if (response.data?.status === "error") throw ...; sendBridgeResponse(..., response.data?.data ?? response.data)`
   —— 它会把 `data` 再脱一层、并把 `status === "error"` 当成失败抛错。
   后端原先自创的 `{code, data, message}` 会被脱层后只剩内层 data（成功尚可），
   失败时语义错乱。

修复：

- 新增 `webui-src/src/bridge.ts`：统一封装官方 bridge（`getBridge/getPageBridge/getContext/onContext`）
  与 **sandbox 安全存储** `storageGet/storageSet`（内部 try/catch，localStorage 不可用时退回内存）。
- `App.vue` 不再碰 `localStorage`；主题初值改为按「用户手选 → `<html data-theme>` → bridge `context.isDark`」
  三级回退，并订阅 `onContext` 跟随面板主题切换、同步页面标题。
- 后端信封改为 AstrBot 约定：成功 `{"status":"ok","data":...}`、失败 `{"status":"error","message":...}`；
  前端 `unwrap()` 改为防御式解包（两种信封都能吃）。
- 顺带修正：`apiGet/apiPost` 之前对**任何**错误都会换 4 种端点写法重试，
  业务错误（如参数校验失败）会被重复打到后端（POST 会重复写库）。
  现在只有「路由不存在 / 404」才换下一种，其余错误立即抛出。

验证：`build_webui.ps1` 重新构建通过（产物 1.37MB / gzip 425KB）；后端信封改动后 `tests/test_store.py` 仍全绿。

## v0.1.0（M0 骨架：元数据、存储层、控制台接口与前端工程）

需求与设计见工作区文档 `docs/astrbot_plugin_user_gateway_PRD.md`。本次是首个可安装版本，
只做基础设施，**尚未接入 LLM 拦截**（那是 M1）。

- 清掉 AstrBot `helloworld` 模板：`metadata.yaml` 落为 `astrbot_plugin_user_gateway` /
  展示名「萌萌权限控制台」/ 作者 windExplorer / `astrbot_version: ">=4.22.0"` /
  `pages: permission-console`；新增 `_conf_schema.json`（17 项配置，含逐项 hint）。
- `store.py`：SQLite（WAL）持久层，建 7 张表 —— `settings` / `friend_cache` / `group_cache` /
  `policy`（预留 `feature` 供 M3 指令权限）/ `llm_quota`（day|month|total）/ `usage_log`
  （统计与明细的唯一事实来源，含 `estimated` 标记）/ `audit_log`；含索引、超期清理、
  好友与群缓存的覆盖式 upsert 与模糊搜索。
- `main.py`：插件生命周期（数据目录 → 建库 → 加载规则到内存 → 清理超期明细 → 注册路由），
  `reload_rules()` 把权限/额度加载进内存缓存（M1 闸门热路径零 IO），
  `quota_reset_at()` 计算日/月重置时间戳；全程 fail-open（任何异常都不影响消息通行）。
- `webui_api.py`：13 条控制台路由（健康检查 / 配置读写 / 总览统计 / 好友群列表 / 权限规则 /
  额度增删改查与清零 / 用量明细 / 审计），统一 `{code,data,message}` 信封；
  配置默认值与类型直接从 `_conf_schema.json` 读取，避免两处漂移。
- 前端 `webui-src/`：Vue3 + TS + Vite + Naive UI + ECharts，hash 路由，单文件产物；
  5 个页面（总览 / 私聊 / 群聊 / 限额 / 配置）+ 深浅主题 + 头像组件（CDN 失败回退首字母色块）；
  ECharts 按需引入以控制产物体积。
- 构建与打包：`build_webui.ps1`（从 `metadata.yaml` 注入版本号后 vite build）、
  `build_zip.ps1`（显式包含清单 + 撞版检测 + 顶层结构自校验，zip 内套插件目录）。
- `tests/test_store.py`：存储层自检脚本（44 项断言），
  用法 `uv run --no-project --with aiosqlite python tests/test_store.py`。
